# PLAN APG / „Mozaffari"-Linie — Prüfung und Nachschärfung, 16.09.2026

> **Herkunft:** Planungsagent (Read-only) gegen HEAD c60190d, 16.09.2026, im Auftrag „APG-Plan prüfen und nachschärfen,
> SISM-Frage mitbehandeln" (TODO.md Abschnitt 1, Punkt 3). Von mir (Hauptsitzung) **selbst im Code nachgelesen** vor dem
> Ablegen: A1 (`yw_ab` in kernel.cpp:2099 gegen `yw` in :2145), die ELIBB×APG-Sperre (lbm.cpp:162, print_error) und A7
> (AUDIT-BEFUNDE.md:478–482, Eichung als Überdehnungs-Artefakt). **Alle übrigen Zeilenangaben und Rechnungen sind
> Agentenbefunde und gehen erst mit der Prüfkette gegen den Diff in den Stand „bestätigt".** Kein Bau, kein Lauf.
> Entscheide E1–E7 (Abschnitt F) sind Heikos.

Fassung: FluidX3D-v2, master, HEAD c60190d. Alle Datei:Zeile-Angaben an diesem Stand nachgelesen (die Zeilen in UEBERGABE-2026-09-14.md §2 und RHO_RAND-PLAN.md — 2109–2136, 2117–2125, 6067 — sind ÄLTERE Fassungen und hiermit ersetzt). Bezugszeile: `logs/kl_std.txt` Zeile `kl_std_pu_b70` (CFD_POSITIV=2 CFD_U_KLEMME=1, RHO_RAND Vorgabe 1, RHO_SPARSAM=1, SISM=1, ELIBB=1, NACHBAR=1, T_END 0,301, VTK_DT 0,15). Bindende Regeln: Iron Rules arm_disziplin, wirkungsnachweis, pruefkette, audit_nach_bauabschnitt (Diagnostik in den Code), messung_vor_vermutung, validierungsreferenz.

## 0 · Ergebnis in drei Sätzen
1. Der APG-Term ist am HEAD ein linearer Dünnschicht-Impulsbilanz-Term (τ_w = Spalding − κ·y·dp/ds), NICHT die Mozaffari-Formel; er trägt zwei Baufehler gegen den heutigen Stand (falsche Höhe y_w statt y_ab unter NACHBAR; 1-Parameter-Fit leckt ∂ρ/∂n am wandgestutzten Stencil) und ist ohne Umbau am Bezug nicht schaltbar (fünf Sperren, drei Variablen, nicht bitreproduzierbar).
2. Bauweg: Nachbar-ρ aus den DDFs in einem eigenen Vorkernel je Facette (fac_nb-Muster), Ausgabe ∇ρ (3 float je Facette). Damit koexistiert APG mit RHO_RAND=1, RHO_SPARSAM=1 UND RHO_FP16 und der A/B trägt EINE Variable. Kosten 4 mm (Rechnung, nicht gemessen): 37,6 MB, ≈4,4 B/Zelle/Schritt roh.
3. κ = 0,5 ist ein Handwert ohne Herleitung (die Kugel-Eichung vom 19.08. wurde am selben Tag als Überdehnungsartefakt eingeordnet). Herleitbar ist nur κ = 1. Die SISM-Frage wird mit zwei Armen (SISM=0; SISM=0+NUT_SKAL=abgelesener Faktor) VOR den APG-Armen auf derselben Bezugszeile beantwortet — VANDRIEST=2 taugt dafür nicht.

## A · Ist der APG-Term richtig gebaut?

**Ort.** Parameter `rho` in `apply_facette_imem`: src/kernel.cpp:2009–2011; Aufrufsplice src/kernel.cpp:3044–3046; Zweig src/kernel.cpp:2127–2154; Slot-8-Umleitung src/kernel.cpp:2105–2114/2119–2121; DIAGZ-Mitschrift `fac_diag[18]` src/kernel.cpp:2888–2890. Host: src/setup.cpp:4350–4352 (Kanal), :5535–5537 (Kugel), :6759–6766 (fahrzeug_dd); JIT-Emission `FACETTEN_APG`/`def_fac_apg` src/lbm.cpp:2345–2346 (nur bei s_facetten && s_fac_imem && κ≠0).

**Formel am HEAD (src/kernel.cpp:2134–2152).**
num = Σ_{ia, Nachbar nicht TYPE_S} w_ia·(c_ia·t1)·(ρ_j − ρ_n), den = Σ w_ia·(c_ia·t1)², dp/ds = num/(3·den) (p = ρ/3, c_s² = 1/3); tw1 = tw − κ·y_w·dp/ds; Klemme [0, 2·tw] → Slot 19 (beide Seiten in EINEM Zähler); danach tw_max-Klemme → Slot 8 (:2151); twe = min(tw·faca, 0,5·ρ·u_t).

**Herleitung/Vorzeichen.** Dünne Scherschicht: ∂τ/∂y = ∂p/∂s ⇒ τ(y) = τ_w + y·dp/ds ⇒ τ_w = τ(y) − y·dp/ds, wobei Spalding die (Gleichgewichts-)Spannung an der Abtasthöhe liefert. Vorzeichen richtig (dp/ds > 0 entlang t1 = u_t/|u_t| senkt τ_w; gefixt nach Prüferbefund 270cd99, AUDIT-BEFUNDE.md:375–377). **Herleitbar ist κ = 1**; jedes κ ≠ 1 ist ein Handwert. Normierung: auf dem vollen D3Q19-Stencil ist Σ w c_a c_b = δ_ab/3, also num/den = t1·∇ρ exakt.

**Was NICHT Mozaffari ist.** Mozaffari–Jacob–Sagaut 2024 (Flow Turbulence Combust 106:3) ist eine beschränkte u_τ-Dämpfung f(α_p) = 1 − C·α_p/(α_p+α_p0) auf α_p = ν·(dp/ds)/(ρ u_τ³), Paperkonstanten C = 0,4, α_p0 = 0,005 — gebaut nur in V1 (/home/heiko/CFD/FluidX3D/src/kernel.cpp:2153–2169, ANDERE FASSUNG), dort −0,87 % Cd (V1-GEGEN-V2.md:38) bzw. +0,03 % ± 8,2 % (UEBERGABE-2026-09-14.md:187). In v2 heißt „Mozaffari-Klasse" (src/lbm.hpp:423) nur die Termklasse. Der Plan behandelt den v2-Term; ob zusätzlich die echte Mozaffari-Form gebaut werden soll, ist eine eigene Entscheidung (E7 unten) — sie braucht u_τ und dp/ds, also dieselbe Vorkernel-Infrastruktur.

**Befunde am HEAD (vor jedem A/B zu beheben):**
- **A1 (HOCH) Falsche Höhe unter NACHBAR.** [von der Hauptsitzung nachgelesen] Seit 03.09. speist `CFD_FAC_NACHBAR=1` (Produktionsstandard) Spalding mit `ut_ab` an der Höhe `yw_ab` (src/kernel.cpp:2076–2078, :2098–2099; y_ab = y_w + c_ib·n, src/kernel.cpp:5558). Die APG-Korrektur nimmt aber `yw` der eigenen Zelle (src/kernel.cpp:2145). Die Bilanz gilt an der Abtasthöhe: Korrektur ist um y_w/y_ab zu klein (bei y_w 0,5 und y_ab 1,5: Faktor 3). APG (18.08.) wurde nie an NACHBAR (30.08./03.09.) angepasst. Fix: `fma(-def_fac_apg*yw_ab, fac_dpds, tw)`; ohne NACHBAR bleibt yw_ab = yw (bitgleich).
- **A2 (MITTEL) 1-Parameter-Fit leckt ∂ρ/∂n.** Am wandgestutzten Stencil ist Σ_{fluid} w (c·t1)(c·n) ≠ 0; ein Normalgradient (Staupunkt, Krümmung) erscheint als Scheinbeitrag in dp/ds. Die Prüfer-Positivliste (AUDIT-BEFUNDE.md:382) deckt nur uniformes ρ (exakt 0). Fix: 3-Parameter-LS (A = Σ w c cᵀ, b = Σ w c Δρ, ∇ρ = A⁻¹b) — auf vollem Stencil identisch zur heutigen Formel, am gestutzten Stencil leckfrei. Passt zum Vorkernel (B), der ∇ρ speichert.
- **A3 Zeitstempel/Determinismus.** rho[j] ist je Scheduling t−1 oder t (src/kernel.cpp:2132–2133); Warnung „NICHT bitreproduzierbar" src/lbm.cpp:220 (Fehlerklasse B72). Wird durch (B) beseitigt.
- **A4 Nachbarwahl.** Übersprungen wird nur TYPE_S (src/kernel.cpp:2138); TYPE_MS (Fluidzellen am bewegten Boden) und TYPE_E werden gelesen — korrekt, beide tragen gültiges ρ. Entartung den < 1e-6 → Korrektur 0 (src/kernel.cpp:2144) OHNE Zähler.
- **A5 Zähler: vorhanden / fehlend.** Vorhanden: Slot 19 (beide Klemmen zusammen), Slot 8 (Nach-APG-tw_max-Klemme), `fac_diag[18]` nur unter DIAGZ — und DIAGZ ist am fahrzeug_dd NICHT verdrahtet (src/setup.cpp:6097 setzt s_fac_diagz = −1; FACETTEN.md „Korrekturen 2026-08-20"). Am Fahrzeug gibt es heute KEINE Zwischenergebnis-Diagnostik zu dp/ds. Fehlend (Iron Rule audit_nach_bauabschnitt): (i) Wirkpfad „APG-Zweig besucht", Soll = [7] − [9] (Facettenbesuche minus u_t<1e-6-Ausstieg, src/kernel.cpp:2058–2059); (ii) Klemme unten / oben getrennt; (iii) Entartung; (iv) Vorzeichenanteil dp/ds > 0 (APG) gegen < 0 (FPG); (v) Histogramm |κ·y·dp/ds|/tw in 8 Dekadenfächern — das ist das „Autoritäts"-Instrument (Lauf-4-Befund: 46 % 0-Klemmen, src/kernel.cpp:2146); (vi) Vorkernel-Ist=Soll (Facetten je Launch = fac_N, ungegatet wie Slot 217). Platz: `hits_n` = 320 (src/lbm.hpp:374), belegt bis 305 (src/kernel.cpp:3131, src/lbm.hpp:375) → **306–319 frei** (14 Slots, reicht: 306 besucht, 307/308 Vorzeichen, 309/310 Klemmen, 311 Entartung, 312–319 Histogramm). Legende an EINER Stelle (src/lbm.cpp ab :692, verbindliche Angabe am Legendenende), Host-Bericht in der Zeile mit „APG-Klemme" (src/setup.cpp:9652).
- **A6 Eingang unter den Klemmen (Stufe-0-Frage aus UEBERGABE-2026-09-14.md:252 beantwortet).** Die Dichteklemme trifft im Standardlauf K0..K4 = 97 / 3 283 833 / 15 / 0 / 0 (logs/kl_std_pu_b70.log:2158–2160): 99,997 % am bewegten Boden (K1), 97 Treffer an Facetten über den ganzen Lauf. Der APG-Eingang ist bei 8 mm praktisch klemmfrei. 4 mm: aus der KLEMM-BILANZ von p4_pu ablesen (unbelegt bis Laufende).
- **A7 κ = 0,5.** [von der Hauptsitzung nachgelesen] Herkunft AUDIT-BEFUNDE.md:463–471 (Kugel-Leiter DX25/DX12, UNGEKAPPT); am selben Tag AUDIT-BEFUNDE.md:478–482: mit Kappung [0, 2tw] fällt der Effekt auf die Basis, „Eichung = Überdehnungsartefakt"; FACETTEN.md:116/360 GEPARKT. Der Wissensspeicher-Satz „κ auf 0,5 geeicht" ist damit eine Notiz über eine widerrufene Eichung. Folge: κ = 0,5 bleibt **deklarierter Interim/Handwert**; der herleitbare Arm ist κ = 1.

## B · Deterministisches Nachbar-ρ mit RHO_SPARSAM=1 und RHO_RAND=1

**Sperren heute:** RHO_RAND × APG src/setup.cpp:5146 (Einzelgitter), :6805 (dd) und src/lbm.cpp:673 (Konstruktor); RHO_SPARSAM × APG src/setup.cpp:6780; RHO_FP16 × APG src/lbm.cpp:230 (Baukonstante src/defines.hpp:22 → anderes Binary); ELIBB × APG src/lbm.cpp:162; PEMA × APG src/setup.cpp:4351/5536/6760 und src/lbm.cpp:231 (bleibt). Ohne Umbau trüge ein APG-Arm gegen die Bezugszeile DREI Variablen (RAND=0, SPARSAM=0, FP32-Build) und wäre nicht bitreproduzierbar (src/lbm.cpp:220) — nach Iron Rule arm_disziplin unzulässig.

**Zensus (gemessen, C0):** APG-Lesemenge N18(L1)\TYPE_S = 1 502 474 Zellen bei 8 mm (logs/kl_std_pu_b70.log:721–725; 6,01 MB FP32), **6 381 760 bei 4 mm** (logs/p4_pu.log:726–730; 25,53 MB FP32); davon 150 578 / 610 678 nur mit Kanten-/Diagonalkontakt (nicht in L1∪L2).

**Weg (a) Region:** FP32-ρ für N18(L1) als Liste mit eigener Bitmaske (Muster alloc_sgs_band), geschrieben von stream_collide im UPDATE_FIELDS-Block. 4 mm: 25,5 MB Region + 40,0 MB Maske (RHO_RAND-PLAN.md:83, F-BBox 1120×470×307) = **65,5 MB**; Verkehr ≈ 0,05 B/Zelle/Schritt schreiben + 1,23 B Maskentest (RHO_RAND-PLAN.md §3) + 0,43 B lesen (18×4 B × 3,13 M / 519 M) ≈ **1,7 B/Zelle/Schritt** (Rechnung). Nachteile: Schreibpfad im heißesten Kernel (Warnung AUDIT-BEFUNDE.md:5555), Wert einen Schritt alt, Befund A2 bleibt ohne Zusatzarbeit.

**Weg (b) Vorkernel `fac_apg_ab` (EMPFOHLEN):** ein Work-Item je Facette (gid = fid, Liste `gd_zellen`), Launch nach `kernel_fac_nachbar` in `enqueue_stream_collide` (src/lbm.cpp:1795), In-Order-Queue → deterministisch (fac_nb-Muster, B70-bewiesen, src/kernel.cpp:5516–5561). Liest an den 18 Nachbarn `load_f(j, ·, t+1)` + `calculate_rho_u` (mit RHO_CLAMP, wie gespeichert) — exakt das Muster von `rho_rek_ebene` (src/kernel.cpp:4745–4748): das ist das ρ, das stream_collide(t+1) an der Nachbarzelle selbst bilden wird, also **synchron** zum eigenen rhon der Facette in t+1 (src/kernel.cpp:2054) — besser als heute (t−1/t-Gemisch). Ausgabe `fac_apg[3·fid]` = ∇ρ aus 3-Parameter-LS über die Fluidnachbarn (behebt A2); `apply_facette_imem` bildet dp/ds = (∇ρ·t1)/3 mit dem lokalen t1 (src/kernel.cpp:2125) und korrigiert mit y_ab (A1). Kein ρ-Puffer wird gelesen → RHO_RAND, RHO_SPARSAM und RHO_FP16 sind unberührt; die Sperren :5146/:6780/:6805/lbm.cpp:673/:230/:220 entfallen. DDF-Rauschen rms 6,06e-6 (gemessen 11.09., UEBERGABE-2026-09-14.md:226) bleibt — galt auch für den alten FP32-Puffer, der aus denselben DDFs kam.
**Die zwei Zahlen (4 mm, Rechnung aus gemessenen Zellzahlen, NICHT gemessen):** Speicher **37,6 MB** (3 float × 3 129 185; skalares dp/ds wären 12,5 MB, aber dann müsste t1 im Vorkernel aus dem u-Feld des Vorschritts kommen — zweiter Zeitstempel, verworfen). Verkehr: Vorkernel 18 × (19×2 B + 1 B Flag) = 702 B je Facette = 2,20 GB/Schritt = **4,23 B/Zelle/Schritt roh** (RHO_RAND-PLAN.md:276: +3,7 %), dazu 0,07 B schreiben + 0,07 B lesen in stream_collide ≈ 4,4 B/Zelle/Schritt. Roh heißt: Cache-Wiederverwendung (jede Fluidnachbarzelle wird von mehreren Facetten gelesen) ist unberücksichtigt und unbelegt → Wanduhr-A/B bei 8 mm messen (Muster C-M5 vom 16.09.: 132,6 gegen 137,5 s, unter der Streuung). Vorsicht Scratch: 3×3-Solve ohne laufzeitindizierte private Arrays (Lehre src/kernel.cpp:5534–5540), scratch_gate.sh über ALLE Kernel.
8 mm zum Vergleich: 719 574 Facetten → 8,6 MB; 505 MB/Schritt roh (7,7 B/Zelle bei 65,6 M Zellen).

**Wenn nicht gebaut wird (Rückfall):** Zwischensprosse Z0 = Bezug + CFD_RHO_RAND=0 CFD_RHO_SPARSAM=0 ohne APG; Soll: forces.csv/cd_facetten.csv bitgleich zu kl_std_pu_b70 (beide Schalter sind als kraft-bitgleich abgenommen: RHO_RAND C2d src/setup.cpp:6790, RHO_SPARSAM 12.09.). Dann ist die Variable „RAND/SPARSAM" isoliert. Bleibt der FP32-Build (RHO_FP16 aus): zweites Binary, Sprosse Z1 = Z0 auf FP32-Build; Kräfte dabei NICHT konstruktiv bitgleich (unbelegt) — und der Arm bleibt nicht bitreproduzierbar. Rückfall kostet zwei Sprossen mehr und liefert einen schwächeren Befund; deshalb (b).

## C · ELIBB × APG
[von der Hauptsitzung nachgelesen: lbm.cpp:162 ist ein print_error] `src/lbm.cpp:162`: `if(s_fac_elibb&&s_fac_apg!=0.0f) print_error("CFD_FAC_ELIBB mit APG ist nicht gebaut -- Messarm rein halten.")` — eine **Sperre aus der Zeit, als ELIBB Messarm war** (25.08.), keine Wechselwirkung im Code: die Blende (src/kernel.cpp:2036–2052) ändert fhn VOR der Abtastung, APG ändert nur das tw-Ziel (2127–2153) und fasst weder fhn noch fac_q an. Einzige Kopplung: das eigene rhon ist post-Blende (src/kernel.cpp:2054), die Vorkernel-Nachbarn sind unverblendet; bei q ≠ 0,5 erhält die Blende die Masse nicht (src/kernel.cpp:4749) → kleiner Versatz in Δρ, mit dem Δm-Wächter (Slot 4, src/kernel.cpp:1982) beziffert und deklariert. `CFD_FAC_ELIBB=2` (PUR) steigt vor dem Wandmodell aus (src/kernel.cpp:2049–2051) → APG wäre stiller No-Op → neue Sperre ELIBB=2 × APG. Da ELIBB=1 in jeder Produktionszeile steht (logs/kl_std.txt, logs/p4_pu.txt), ist das Aufheben Voraussetzung jedes APG-A/B — **Entscheid Heiko (E2)**, Bau: print_error → print_info mit Ansage, Nachweis an der Kugel (ELIBB=1×κ vs ELIBB=0×κ vs ELIBB=1×0: Slot 306 feuert, Δm unverändert, keine NaN).

## D · SISM-Frage: Modellphysik oder fehlende Wanddämpfung?
Stand: Diskriminator `CFD_SGS_NUT_SKAL` ist gebaut (src/kernel.cpp:3494–3527, Heiko-Go 10.09.), Sperren src/lbm.cpp:199–206; Wirkpfad 188 = Slot 76, Fächer 191–198. Abgenommen NUR am Kanal N=20 (logs/ns_neu_serie.txt, ns2_serie.txt); **am Fahrzeug nie gefahren** (keine Logs → unbelegt). Rückkopplung: sgs_fdwand src/kernel.cpp:5439 → fac_wfd → stream_collide src/kernel.cpp:2983/3441 im Folgeschritt; APG ändert τ_w → u → |S|_FD → w; u-Klemme [296] an derselben Lage.
**VANDRIEST=2 bewertet: ungeeignet.** (i) D²(y⁺) aus dem τ_w-Laufmittel ist ein y⁺-abhängiger, kein konstanter Faktor — ein zweites Modell, nicht „gleiche Dämpfung ohne Scherungssubtraktion"; (ii) am Kanal gestrichen (falsches Vorzeichen, GRENZSCHICHT-SGS-PLAN.md:267–277), bei 4 mm −23,6 % ν_t an Facetten bei 0,1 σ Kraftwirkung (AUDIT-BEFUNDE.md:5811–5812); (iii) der Kommentar src/kernel.cpp:3505 nennt VANDRIEST=2 nur als ausgeschlossenen zweiten Senker. Der im Kommentar skizzierte Ansatz IST NUT_SKAL.
**Messdesign (8 mm, Bezugszeile A0, je EINE Variable):** S1 = CFD_SGS_SISM=0 (klassisches FDWAND); S2 = SISM=0 + NUT_SKAL=f. f ist ABZULESEN, nicht 0,148: die 85,2 % sind 4 mm (AUDIT-BEFUNDE.md:5809), bei 8 mm 82,3 % → f = 0,177 — beides Stand 08.09., vor Klemmen-Standard; der Faktor ist auf der NEUEN Zeile neu zu messen (Instrument der Lagenmessung 08.09. identifizieren, Kandidat SGS-BAND/GDIAG-Bericht — ob es in der Standardzeile mitläuft, ist unbelegt; sonst Sprosse S0g = A0 + Instrument, Kräfte bitgleich). Kriterium: Δ(A0−S1) und Δ(S2−S1) in cd_druck_rest UND cz_druck_rest mit dem 50-Sample-Vorzeichentest; reproduziert S2 die A0-Verschiebung → SISM-Gewinn = Dämpfung; bleibt S2 bei S1 → Scherungssubtraktion ist Physik. Zweite Observable: Ausreißer-Feldprüfung (werkzeuge/feldscan.py; 84,6 % Wandnachbarn, src/kernel.cpp:3498–3499). Vorbehalt: die 8-mm-Sprosse hat bei SISM schon getäuscht (Druckwiderstand −0,51 bei 70 σ, bei 4 mm null; AUDIT-BEFUNDE.md:5813) — Bestätigung bei 4 mm nur mit Go. Reihenfolge: S1/S2 VOR A1/A2 — sie brauchen keinen APG-Code, nur die freie Queue, und ihr Ergebnis bestimmt, ob APG später zusätzlich auf S1 (SISM=0) zu messen ist.

## E · Messdesign 8 mm
**Bezugszeile:** kl_std_pu_b70 (logs/kl_std.txt). Serienweit deklarierte Abweichungen (in ALLEN Armen, auch A0): `CFD_VTK_DT=0.025` und `CFD_T_END=0.501`. Grund 0,025: Ablöseort-Messung `werkzeuge/abl_dach` (fx_band.py → fx_profil2.py, 7 Kriterien, OF13 anliegend bis 3,66 m) streut mit VTK_DT 0,15 (3 Felder) mit SD 0,27 m = 5× Nachweisschwelle; erst 12 Felder sind messbar (UEBERGABE-2026-09-14.md:244–246). Die 12 Felder setzen ein Fenster 0,2–0,5 s voraus — mit T_END 0,301 wären es 4; deshalb T_END 0,501 (Entscheid E4). Kosten: 135,8 s Wanduhr je 200 ms (logs/kl_std_pu_b70.log:2106) → ≈340 s je Arm (Rechnung); Platte: VTK_DT tickt ab 0 (Dumps bei 150/300 ms belegt), 20 Dumps × (1,11 GB nah + 0,42 GB fern, export/kl_std_pu_b70) ≈ 31 GB je Arm — Platz prüfen; ob CFD_VTK_STRIDE>1 für abl_dach zulässig ist, in fx_band.py nachlesen (unbelegt).
**Arme (je EINE Variable gegen A0, ein Binary, gelockte Queue):**
| Arm | Änderung | Soll |
|---|---|---|
| A0 | Bezug, neues Binary, κ=0 | forces.csv/cd_facetten.csv bitgleich zu kl_std_pu_b70 über 0–0,301 s (beweist Inertheit des ganzen APG-Baus UND VTK-Neutralität); Slot 306 = 0 |
| S1 | SISM=0 | Abschnitt D |
| S2 | SISM=0, NUT_SKAL=f | [188] = [76]; Abschnitt D |
| A1 | κ=1 | [306] = [7]−[9]; Histogramm 312–319; Δm; keine NaN; Budget; [296] gegen A0 |
| A2 | κ=0,5 (Interim, historisch) | wie A1; A1 gegen A2 liefert die κ-Antwort am Fahrzeug, die die Kugel nicht liefern konnte |
Voraus: Gate (scratch_gate.sh, 58 Arme × 39 Kernel rc 0), Kugel-Leiter sechs Hashes bitgleich bei κ=0 (Referenzen logs/kl_a8_ku_*), CPU-Harness-Selbsttest des Vorkernels (uniformes ρ → ∇ρ = 0 exakt; lineares ρ → ∇ρ exakt; Legacy-Pfad vs Vorkernel innerhalb DDF-Rauschen, nicht bitgleich — anderer Zeitstempel).
**Kraftgrößen:** cd_druck_rest, cz_druck_rest (momentan je Sample, cd_facetten.csv Kopfzeile „DREI ZEITBASEN"), cd_reib/cz_reib (Fenstermittel), Band gegen Rest getrennt aus kraft_zband.csv (KRAFT_ZBAND=2, 16-mm-Gitterband — zwischen DX-Sprossen nicht vergleichbar), Ablöseort aus abl_dach. **Kriterium (16.09.):** 50 Samples 0,202–0,300 s (vergleichbar zu den heutigen Zahlen) und zusätzlich 150 Samples bis 0,500 s: Differenz ± sd, Zahl der Samples mit umgekehrtem Vorzeichen (0/50 = systematisch), Blockmittel je 20 ms; σ(cd_rest, Block-SEM 4) nur als Maßstab. Kein Schwellwert für „wie viele Vorzeichenwechsel sind Rauschen" — die Zahl wird berichtet. **Autorität:** Anteil der Facetten mit |κ·y·dp/ds|/tw ≥ 1 (Fächer 318/319) im Bericht; liegt die Korrektur überwiegend in der Klemme, ist κ nicht deutbar (Augustlehre). **Erwartung: unbelegt** — August: null mit ehrlicher Kappung an der Kugel, am Fahrzeug wirkungslos, aber auf alter Architektur und ohne A1/A2-Fix.

## F · Reihenfolge, Aufwand, Entscheide
**Jetzt, ohne GPU (kein make, solange p4_pu läuft — Lehre 16.09.):** Diff vorbereiten: (1) Vorkernel `fac_apg_ab` nach src/kernel.cpp:5561, Puffer `fac_apg` 3 float je Facette in alloc_facetten_domain (Muster src/lbm.cpp:1652–1669: alloc, delete_host_buffer nur dGPU, Kernel binden, Rebind von stream_collide, Positionsarithmetik `fac_param_pos` :1667/:1692 erweitern), Launch src/lbm.cpp:1795, VRAM-Vorschätzung src/lbm.cpp:1463 mitziehen (KDIAG-Lehre); (2) `apply_facette_imem`: `rho`-Parameter (:2009–2011, :3044–3046) durch `fac_apg` ersetzen, Zweig :2127–2153 auf ∇ρ·t1/3 und y_ab umstellen; (3) Zähler 306–319 + Legende + Host-Bericht; (4) Sperren: lbm.cpp:162 → Info + ELIBB=2-Sperre; :220/:230, :673, setup.cpp:5146/6780/6805 entfallen; PEMA-Sperren bleiben; (5) CPU-Harness-Selbsttest. Dann Iron Rule pruefkette: unabhängige Prüfung gegen den Diff VOR jeder Messung. Aufwand: ~150–250 Zeilen Gerät+Host, ein Bauabschnitt, danach Audit-Schleife.
**Freie Queue, kurz (~15 min):** Gate, Kugel-Hashes, A0.
**Freie Queue, Serie (~30 min):** S1, S2, A1, A2.
**Braucht Heikos Entscheid:** E1 Weg (b) Vorkernel statt (a) Region (Empfehlung b); E2 ELIBB-Sperre aufheben (Policy „Messarm rein halten"); E3 κ=1 als Hauptarm, κ=0,5 nur als deklarierter Interim mitfahren oder streichen; E4 T_END 0,501 + VTK_DT 0,025 serienweit (Zeit, ~31 GB je Arm); E5 SISM-Diskriminator (S1/S2) vor den APG-Armen; E6 Legacy-APG-Pfad (rho im Kernel) löschen oder eine Sprosse als Harness-Referenz behalten; E7 echte Mozaffari-Form (u_τ-Dämpfung, V1-Konstanten) als späteren eigenen Arm auf derselben Infrastruktur — nicht in dieser Serie. 4 mm nur mit Go, nur für validierte Gewinner, Platte 12+ VTK × 8,8 GB (UEBERGABE-2026-09-14.md:265).

## Kritische Dateien
- /home/heiko/CFD/FluidX3D-v2/src/kernel.cpp (APG-Zweig 2009–2011, 2105–2154, 3044–3046; fac_nachbar_ab 5516–5561 als Muster; rho_rek_ebene 4735–4809; SISM/NUT_SKAL 3409–3529)
- /home/heiko/CFD/FluidX3D-v2/src/lbm.cpp (Sperren 162/219–231/673; fac_nb-Alloc 1652–1669; Launch 1795; JIT 2345–2346; Legende ab 692)
- /home/heiko/CFD/FluidX3D-v2/src/setup.cpp (Lesestellen 4350–4352, 5146, 5535–5537, 6759–6766, 6780, 6805; Bericht 9652)
- /home/heiko/CFD/FluidX3D-v2/logs/kl_std.txt (Bezugszeile) und logs/kl_std_pu_b70.log (Zensus 721–725, Klemm-Klassen 2158–2160, Wanduhr 2106)
- /home/heiko/CFD/FluidX3D-v2/AUDIT-BEFUNDE.md (370–382, 463–482, 5800–5815) und UEBERGABE-2026-09-14.md §2 (178–265)

## G · Umsetzung 16.09.2026 (Heiko: „immer der billigste in Aufwand und VRAM/Performance; Reihenfolge bei Dir")

**Entscheide, wie ich sie nach diesem Kriterium getroffen habe:**
- **E1 → Vorkernel, aber in der schlanksten Form:** `fac_apg_ab` (src/kernel.cpp, eigener Kernel je Facette, Launch direkt nach
  `fac_nachbar_ab`) liest die DDFs der **sechs Achsnachbarn** und der Zelle selbst (7 × 39 B = 273 B je Facette, ein Drittel des
  18-Nachbarn-Entwurfs), bildet ∇ρ als zentrale Differenz — einseitig, wo ein Nachbar Solid ist, 0 auf einer Achse ohne Fluidnachbar
  ([307]) — und schreibt es nach `fac_nb[5f+2..4]` (Stride 2 → 5 nur unter APG, `def_nb_stride`, unbedingt emittiert). Kein 3×3-Solve,
  kein Scratch-Risiko, dieselben 37,6 MB bei 4 mm wie (b). Erst als gemeinsamer Kernel mit `fac_nachbar_ab` gebaut — das Gate meldete
  1280 B Spill auf der B70 (private 7296 in der Schleifenfassung = die Scratch-Lehre vom 11.09.), darum eigener Kernel mit sechs
  ausgeschriebenen Literalblöcken und Hilfsfunktion `apg_rho_zelle` (load_f(t+1) + calculate_rho_u, wie `rho_rek_ebene`).
- **E2 → ELIBB-Sperre aufgehoben** (lbm.cpp), ELIBB=2 (PUR) × APG neu gesperrt (APG wäre dort stiller No-Op). **Neu: APG braucht
  `CFD_FAC_NACHBAR=1`** (Korrektur an y_ab, ∇ρ aus fac_nb).
- **E3 → κ=1 Hauptarm, κ=0,5 ein Zusatzarm.** **E4 → nein**, die lange Serie (T_END 0,501 + VTK 0,025, ~31 GB je Arm) nicht in
  dieser Runde; Ablöseort nur, wenn die Kräfte etwas zeigen. **E5 → nur S1** (SISM=0), S2 erst bei Bedarf. **E6 → Legacy-Pfad
  gelöscht** (der `rho`-Parameter von `apply_facette_imem` und der In-Kernel-Fit; rho-Typ-Zensus Soll 15 → 14). **E7 → nicht jetzt.**
- Sperren entfallen: RHO_RAND × APG (setup.cpp 2×, lbm.cpp), RHO_SPARSAM × APG, RHO_FP16 × APG, „nicht bitreproduzierbar".
  Befund A1 (y_ab) und A2/A3 (Gradient aus Vorkernel) sind damit behoben.

**Diagnostik (Iron Rule 3), Slots 306–316:** [306] Vorkernel-Besuche (Soll [7]), [307] entartet, [308] APG-Zweig besucht
(Soll [7]−[9]), [309]/[310] Klemme unten/oben (Summe = [19]), [311]/[312] Vorzeichen dp/ds, [313..316] Autorität
|κ·y_ab·dp/ds|/tw in <0,1 / 0,1–0,5 / 0,5–1 / ≥1. Bericht `berichte_apg` (setup.cpp) mit Ist=Soll, Befunde gesammelt
(`apg_verletzt`, Abbruch am Fallende). Testhaken `CFD_FAC_APG_HAKEN`: 1 = ∇ρ-Statistik (Host-Puffer bleibt), 2 = Konstantgradient
(1e-3, 0, 0) → bitgenaues Soll. Gate: Arme `prod8naha`/`prod8nahA`.

**Belege 16.09.:**
- Gate: 62 Arme × 39/40 Kernel rc 0, `fac_apg_ab` private 0 / spill 0 auf iGPU und B70 (`logs/gate_apg_16-09.log`).
- CPU-Kugel 40 mm ohne Facetten (`kl_a9_ku_*`): alle sechs Hashes exakt wie die Referenzen — der Umbau ist außerhalb des
  APG-Pfads bitneutral.
- CPU-Kugel 25 mm mit Facetten + NACHBAR + ELIBB (`ka_ku25_*`): a0 (κ=0) Hash 10537532691050520009; **Haken 2:** alle 1706 Facetten
  tragen (1e-3,0,0) bitgenau, [306] = [7] = 6824, [308] = [7]−[9] = 6822, [309]+[310] = [19]; **Haken 1:** echte Gradienten,
  Mittel 0,0060, Maximum 0,0536 je Zelle, keiner exakt 0; h1 und k1 hashgleich (16922185928262983113 — der Haken 1 ändert nichts).
- **Physikbefund an der 25-mm-Kugel, κ=1:** Autorität ≥1 in **97,8 %** der Besuche — die Korrektur liegt fast überall in der
  Klemme [0, 2·tw] (3008 unten / 3663 oben von 6822). Genau die Augustlehre, jetzt vom eingebauten Instrument beziffert: an dieser
  groben Kugel (y_ab ≈ 1,5 Zellen = 37 mm) ist κ nicht deutbar. Am Fahrzeug (8 mm, y_ab ~12 mm) ist das die erste Zahl, die zu lesen ist.
- 8-mm-Regression (`kl_apg_reg_b70`, Standardzeile mit NACHBAR=1, kein APG): läuft; Soll bitgleich zu `kl_std_a_b70`.


## §H Ergebnis 16.09. 12:41 (8 mm, B70, Bau 63b6575; Prüfagent Durchgang 2: kein HOCH/MITTEL, drei NIEDRIG ohne Codeänderung — Paritätsbegründung Init-Launch korrigiert, Stride-Wächter setup.cpp:1383 tautologisch, HAKEN 3 fiele bei Facetten am periodischen Rand LAUT durch)

- Bau abgenommen bis Durchgang 1 + Korrekturen (HOCH-1 Instanz-Einfrieren, HAKEN 3 analytisch 1706/1706, dd-Beleg `apgdd_k1_igpu` [306]=[7]).
- **A1 (κ=1) und A2 (κ=0,5) gegen A0: keine Kraftwirkung** — cd_druck_rest −0,009 ± 0,004 bzw. −0,017 ± 0,003 (Zeitreihentest: Rauschen/abklingend),
  cz_druck_rest Rauschen; cd_reib/cz_reib bei κ=0,5 systematisch −0,0008 (−2,3 % der Reibung, 0/50).
- **Autorität ≥ 1 in 98,5 % / 97,6 %**: das Ziel liegt fast überall auf der Klemme [0, 2τ_w] — der Term ist bei y_ab ≈ 8 mm um Größenordnungen
  größer als τ_w. Die Augustlehre gilt auch mit deterministischem Gradienten; κ ist nicht deutbar.
- **Kosten −17,6 % Durchsatz** (5289 → 4356 MLUPs, +42 s auf 232 s je 301-ms-Lauf).
- **Empfehlung: APG PARKEN** (Code bleibt, Standard `CFD_FAC_APG=0`), kein 4-mm-A/B (≈100 min GPU für einen klemmdominierten Term; das Verhältnis
  halbiert sich bei 4 mm nur — Rechnung). Wiederaufnahme nur mit einer Formulierung, die den Term auf seinen Gültigkeitsbereich begrenzt
  (|κ·y·dp/ds| < τ_w, heute 1,5 % der Besuche) — das wäre eine neue Modellentscheidung, nicht ein Nachschärfen.
- Protokoll: wissensspeicher-tagesprotokoll/2026-09-16.md 12:41; Serien `logs/apg8_teil1.txt`, `logs/apg8_teil2.txt`.


## §I Ergebnis 4 mm (`p4_apg1`, 16.09. 13:11–14:15, κ=1 gegen `p4_pu8`)

Wirkpfad belegt ([306]=[7]=159 588 435, [308]=[7]−[9], [309]+[310]=[19]), Abnahme sauber. **Keine Kraftwirkung:** cd_druck_rest
−0,0022 ± 0,0011 (150/300, 41 Vorzeichenwechsel), cz_druck_rest +0,0093 ± 0,0021 — beides Rauschen; gegen `p4_register` ebenso.
Einzige systematische Wirkung: cz_reib +0,0007 (300/300, 0 Wechsel) = +0,96 % der Reibung. **Autorität ≥ 1 in 96,8 %** (8 mm: 98,5 %)
— das halbierte y_ab senkt den Klemmenanteil um 1,7 Punkte, der Term bleibt klemmdominiert. Kosten: +17,0 % Wanduhr (47,9 → 56,0 min),
−14,6 % Durchsatz, +55 MB VRAM.

**NACHTRAG 22.09.2026:** Die lineare Form ist gemessen ein Vorzeichendetektor (Autorität ≥1 in 97,1 %, Tagesprotokoll 2026-09-22 §B15). Der Formeltausch auf Mozaffari (`CFD_FAC_APG_MOZ=1`, E7 aus diesem Plan) ist gebaut, zwei Prüfdurchgänge, Details FACETTEN.md Schaltertabelle; der 8-mm-Arm steht aus. Der folgende Absatz gilt für die LINEARE Form.

**Damit ist die APG-Linie (lineare Form) abgeschlossen. Empfehlung: endgültig parken** — `CFD_FAC_APG=0` bleibt Standard, der Code bleibt stehen
(Vorkernel, HAKEN 1/2/3, Zähler [306..316] sind abgenommen und wiederverwendbar, falls der Term je auf seinen Gültigkeitsbereich
begrenzt werden soll). Eine Wiederaufnahme müsste zuerst zeigen, dass τ_w überhaupt ein Hebel auf den Druckwiderstand ist — bei 8 mm
und 4 mm ist er es nicht.
