{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -e '((.tool_input.command // \"\") | test(\"lauf_queue\")) and ((.tool_input.run_in_background // false) | not)' >/dev/null && echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"IRON RULE (MEINE-FEHLER.md): lauf_queue.sh-Serien laufen NUR mit run_in_background:true. Aufruf mit run_in_background:true wiederholen.\"}}' || true",
            "timeout": 10,
            "statusMessage": "Queue-Wächter prüft"
          }
        ]
      }
    ]
  }
}
