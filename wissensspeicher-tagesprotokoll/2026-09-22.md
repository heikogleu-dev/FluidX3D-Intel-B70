# Tagesprotokoll 2026-09-22

Auftrag Heiko (morgens): Übergabe vom 21.09. abends lesen und mit dem Rekonstruktionsplan
selbstständig weitermachen.

Ausgangslage geprüft: kein Lauf aktiv (Prozess-Census leer), Platte 288 GB frei,
HEAD `082a54d`, Arbeitsbaum sauber bis auf `werkzeuge/bereinigung_20260921_liste.txt` (unversioniert).

---

## B1 · `CFD_FAC_KRAFT=1` am Fahrzeug IST gemessen — Plan §0/§11 ist falsch

`REKONSTRUKTION-PLAN.md:19` führt als *Korrektur* von Fassung 1 an, der Schalter sei „am Fahrzeug
**nie gemessen**", und §11 macht daraus Schritt 1 („billigste offene Messung im ganzen Feld").
Beides trägt nicht.

**Gemessen am 30.08.2026**, gepaart, identisches Binär, eine Variable:
`export/w_ref` gegen `export/w_kraft1`, Commit `53d6c65` (Arbeitsbaum schmutzig, `aenderungen.diff`
liegt bei), 8 mm, `fahrzeug_dd`, 151 Messpunkte ab t ≥ 0,2 s.

Nachgerechnet aus `cd_facetten.csv` (Facettenpfad, nicht `object_force`):

| Größe | `w_ref` | `w_kraft1` | Δ ± SEM | σ | Urteil |
|---|---|---|---|---|---|
| `cd_druck_rest` | 1,0568 | 1,2880 | **+0,2313 ± 0,0102** (gepaart, r=0,497) | 22,7 | unterscheidbar |
| `cz_druck_rest` | −0,1199 | −0,0906 | +0,0293 ± 0,0275 (ungepaart, r=0,176) | 1,06 | **nicht von Rauschen zu trennen** |

Im Speicher liegt der Befund als `messung_kraftleiter` (provenance `messung`, 30.08.2026) mit dem
Verdikt: **„Weg F ist ein validiertes NEGATIV"** — das Modellziel an Treppen-/Rückfallzellen exakt
durchzusetzen *erhöht* die Reibung, reines BB lag näher am Ziel als das Modell.

**Was das für den Plan heißt:** Schritt 1 ist keine offene Messung, sondern die Wiederholung eines
bekannten Negativs. Die Größe, die der Plan schließen will (Cz), bewegt der Schalter mit 1,06 σ
nicht; die Größe, die ohnehin nahe der Referenz liegt (Cd), verschlechtert er um +21,9 %.

### Zwei Vorbehalte, die ich nicht unterschlage

1. **Bandartefakt:** beide Arme fuhren `CFD_KRAFT_ZBAND=2`, tragen also die Deckellage im Rest
   (`BAND-ARTEFAKT-8MM.md:52`, w_kraft1 und w_ref beide als „cd/cz_rest verschmutzt" markiert).
   Wieviel der Armdifferenz Keil-/Deckelkraft ist, ist **ungeklärt** — derselbe Vorbehalt, den das
   Dokument für den D3Q27-A/B vom 17.09. notiert. Die Kennzeichnung entwertet die Differenz nicht,
   sie beziffert sie nur nicht.
2. **Fassungsabstand:** zwischen `w_ref` (30.08.) und dem heutigen Stand liegen APG, DETEPS,
   NACHBAR (ersetzt das damalige `CFD_FAC_UTKORR=1.5`), PINV, P-TRT 1,90, POSITIV=2, U_KLEMME=1,
   SISM, `CFD_Y_VERSATZ=1` und die korrigierte ZBAND-Regel (Diff der `LAUF.txt`-Umgebungen).
   Der Befund gilt für den Stand vom 30.08., nicht ungeprüft für heute.

### Was statt dessen offen ist — und *dieser* Arm entscheidet wirklich

`CFD_FAC_KRAFT` hat zwei Stufen (`lbm.hpp:403`): **1 = an Rückfallzellen, 2 = an allen
Facettenzellen**. Am Fahrzeug existiert nur Stufe 1. Zensus über alle 779 `export/*/code/LAUF.txt`:

    KRAFT=2  →  nur Kanal (x0_kipp0_kraft2, x26_kraft2, x45_kraft2, jit_kipp26_kraft2,
                jit_igpu_kipp26_kraft2, vt_kdiag_kraft2)
    KRAFT=1  →  Kanal + Fahrzeug 8 mm (w_kraft1 mit Messwerten; vp_kraft8, za_kraft1, zb_kraft1
                brachen vor 0,2 s ab)

Die Rekonstruktion zielt laut `REKONSTRUKTION-PLAN.md:137-139` auf einen **schaltbaren Umfang**
`Rang≤1 / Rang≤2 / alle`. Für den Umfang „alle" ist die passende Obergrenze **KRAFT=2**, nicht
KRAFT=1. Nur dieser Arm ist am Fahrzeug ungemessen und beantwortet die Frage, die der Plan
stellt: *kann eine exakte Durchsetzung des Wandmodellziels an allen Facettenzellen die
Abtriebslücke überhaupt bewegen?*

**Entscheidungsvorlage für Heiko steht unten unter „Offen".**

---

## B2 · S−1 gebaut: Slotpuffer 320 → 384, und ein Defekt dabei gefunden

Plan-Schritt 4 (`REKONSTRUKTION-PLAN.md` §5 Stufe S−1). Der Kernel indiziert `rho_clamp_hits[]`
mit **Literalen** und kennt keine Schranke; ein Zähler oberhalb der Pufferlänge schreibt aus dem
Puffer heraus, ohne dass irgendetwas meldet. Frei waren 3 Slots (317..319), der Plan braucht 12–14.

**Geändert** (`lbm.hpp:382`, `lbm.hpp:383`, `lbm.cpp:727`, `lbm.cpp:748`, `setup.cpp:1425`):

**DEFEKT, den der Plan nicht kannte:** `setup.cpp:1425` führte die Slotzahl **hart als 320**
(`ulong v[320]; for(uint k=0u; k<320u; k++)`), obwohl `lbm.hpp:382` sie ausdrücklich „an EINER
Stelle" führt. Ohne den Fix hätte der APG-Bericht nach der Erhöhung alle künftigen Zähler ab 317
stumm übergangen — dieselbe Klasse wie Prüfbefund 3-E vom 25.08.

**Bitgleichheit, strukturell belegt statt behauptet:**
- `src/kernel.cpp` unverändert (`git diff --name-only src/kernel.cpp` leer)
- `hits_n` kommt in `kernel.cpp` und `defines.hpp` **nicht vor** → keine Define-Emission
- beide `lbm.cpp`-Änderungen sind reine Kommentare (Codeteil von `:727` unverändert)
- kein Slotindex angefasst
- `scratch_gate.sh`: **Exit 0**, 39 Kernel × 30 Arme (offline, ocloc, keine GPU)

**Prüfagent (unabhängig, gegen den Diff): kein HOCH, kein MITTEL.** Zusätzlich belegt:
- Puffer wird genullt (`opencl.hpp:546-550`, `reset()` im Memory-Konstruktor) → 317..383 sind exakt 0,
  der Wickelwächter (`setup.cpp:1926`, iteriert über `.length()`) bildet nur Maxima und meldet nicht
- `capacity()` = 1536 B, Vielfaches von 64 → Zero-Copy-Bedingung hält
- `memory_used` += 0 MB → kein VRAM-Budgetbeitrag
- höchster wirklich benutzter Slot ist **316** (`kernel.cpp:2142`, `hits[hb_]` mit hb_ ≤ 316),
  alle Basis+Offset-Formen aufgelöst; „nächster freier 317" bestätigt
- keine zweite harte Slotzahl im Baum; kein Python-/Shell-Auswerter kennt die Slotzahl

**Drei Kosmetika des Prüfagenten eingearbeitet** — zwei davon sind eigene Rechenfehler im Kommentar,
den ich eine Stunde vorher geschrieben habe:
- „9,2 kB Hoststack" war falsch (rechnete 24 B je Slot); der Slot kostet 4+8+8 = **20 B** →
  Arrays 6400 → 7680 B, `sizeof(KlemmBilanz)` 7520 → **8800 B**, Zuwachs **1280 B je Instanz**,
  im dd-Fall zwei Instanzen = 2560 B gegen 8 MiB Threadstack. Dieselbe falsche Zahl stand in
  `REKONSTRUKTION-PLAN.md` §7 und ist dort mitberichtigt.
- „256 B gesamt" ist **256 B je Domäne**.
- Plan §7 führte noch „Puffer 320, drei frei" — nachgezogen.
- Der im Plan unter S−1 geführte `scratch_gate.sh`-Arm **gehört nach S0**: S0/S1 steuert laut
  derselben Tabelle über einen *Laufzeit*-Parameter, in S−1 gibt es also kein Define, für das ein
  Arm zu bauen wäre. Im Plan an beiden Stellen umgehängt.

---

## B3 · Übergabe §5: Plattenplatz und lautes Scheitern — Teil 1 (Queue)

Zwei Punkte standen aus dem Schaden vom 21.09. offen. In `werkzeuge/lauf_queue.sh` eingebaut:

1. **Plattenstand vor und nach jedem Lauf** in der Statusdatei (`df -P --output=avail`), dazu die
   verbrauchten GB. Bei 0 Bytes frei eine ausdrückliche Warnung, dass jede weitere CSV-Zeile still
   verloren geht (ofstream badbit).
2. **Die stille Gatterung der Kraftbilder** (`[ -s export/<lauf>/cd_facetten.csv ]`) sagt jetzt,
   warum sie nichts tut, statt die Bilder kommentarlos ausfallen zu lassen.

**Einen dritten Wächter habe ich gebaut, getestet und wieder verworfen** — der Test gehört ins
Protokoll, weil er die Schwelle widerlegt:

Idee war, einen stillen Ausgabeverlust an der Dateizeit zu erkennen: „CSV, die länger als 10 % der
Laufdauer vor dem Laufende zuletzt geschrieben wurde". Am Schadensfall funktioniert das:

    p4_regel6, Lag gegen die letzte Ausgabe:  14 CSV bei 2083 s, die übrigen bei 0..310 s
    (forces, kraft_zband, interface_druck, band_bilanz, klemm_budget, yplus_*, … alle 2083 s)

Am **gesunden** Lauf `p4_regel7` schlägt er aber falsch an: `facetten_histogramme.csv` wird einmal
beim Facettenaufbau geschrieben und liegt dort **8426 s** vor Laufende. Ein Wächter, der am guten
Lauf meldet, wird weggesehen — und dann meldet er auch beim schlechten umsonst. Entfernt; die
Begründung steht im Skript an der Stelle, damit sie niemand ein zweites Mal baut.

**Die tragfähige Prüfung ist der Stream-Zustand im Code** (`ofstream` `fail`/`bad` nach jedem
Schreibblock) und eine Bedarfsschranke vor dem ersten Zeitschritt, wo Gittergröße, `CFD_VTK_DT` und
`CFD_T_END` bekannt sind — `setup.cpp:8642` rechnet die Dumpgröße dort bereits exakt vor. Das ist
Teil 2 und steht noch aus.

---

## B4 · Plan-Schritt 3 (FP16S-Rauschlast) erledigt — ohne eine Zeile zu rechnen

Der Planungsagent hat den Schritt vor dem Bau zerlegt, ich habe alle vier tragenden Belege selbst
nachgeprüft. Ergebnis: **Schritt 3 entfällt, und seine Begründung war falsch gerechnet.**

**1. Der Nenner im Plan ist falsch.** FP16S speichert nicht f, sondern die Störform (f_i − w_i)·2^15
als IEEE-half — `lbm.cpp:2409-2413` (`#define fpxx half`, `store(p,o,x) vstore_half_rte((x)*32768.0f,o,p)`),
`defines.hpp:27` FP16S an, `defines.hpp:80` FP16C **auskommentiert**. Das Quantum ist relativ zu
|f_i − w_i|, nicht zu f_eq ≈ w_i.

*Eigene Korrektur:* ich hatte vormittags mit 11 Mantissenbits und 2^−12 gerechnet — das ist das
**custom** FP16C-Format (1-4-11, `kernel.cpp:862`), und das ist toter Code. Gebaut ist die IEEE-half
1-5-10, also **10** Mantissenbits: halbe ULP = 2^−11 = 4,9e-4.

**2. Die Zahl, gemessen an echten Feldern** (1800 Facettenzellen, u/ρ/flags aus
`export/p4_regel7/feld_nah_001001ms.vtk`, f_neq per Chapman-Enskog):

    R = |f_neq| / |f − w_i|      Median 0,265   q10 0,020
    Rauschlast 2^-11 / R         Median 0,18 %  q10 2,4 %
    Rauschlast nach der Plan-Norm       10,2 %

**Faktor ≈ 55 zu pessimistisch.** Die Plan-Norm hätte aus einem Rechenfehler heraus für Variante B
gesprochen. Die CE-Schätzung lässt die Wandmodell-Geistermoden weg → wahres |f_neq| größer,
Rauschlast noch kleiner; der Fehler geht in die sichere Richtung.

**3. Zweimal GEMESSEN beantwortet, ohne dass es jemand als Antwort geführt hätte:**
- `AUDIT-BEFUNDE.md:940-945` — Dreiarm-A/B FP32/FP16C/FP16S, 8 mm, 150 gepaarte Stichproben: gegen
  FP32 ist `cz_druck_rest` unter FP16S **+0,0038 bei 0,70 σ**, Vorzeichentest 69/150 = Zufall.
  Das gesamte FP16S-Rauschen *abzuschalten* bewegt den wandmodelldominierten Koeffizienten nicht.
- `WANDMODELL.md:219` — Kanal, „FP16C-Quantisierung", Test „FP32-Build, gleicher Lauf": tot, −3 %
  auf c_f. Anderes Format als heute gebaut, daher Zweitbeleg.

**4. Falsche Herkunftsangabe im Code berichtigt.** `kernel.cpp:3882` führte „relative FP16S-Auflösung
9,8e-4 … **(gemessen, AUDIT-BEFUNDE B77)**". B77 (`AUDIT-BEFUNDE.md:5499-5520`) misst etwas anderes
(cz_druck_rest unter **FP16C**, −0,0119 bei 2,17 σ); die 9,8e-4 sind dort **hergeleitet**. Dazu:
9,8e-4 ist die *volle* ULP, der echte Worst-Case-Rundungsfehler ist die halbe = 4,9e-4 — Slot 203
**untertreibt** also um Faktor 2. Die Gegenstelle `kernel.cpp:5107-5109` nennt richtig 2^−11 = 4,9e-4;
zwei verschiedene Zahlen für dieselbe Sache in derselben Datei.
**Die Schwelle bleibt trotzdem stehen** — sie zu ändern bräche die Vergleichbarkeit zu allen
bisherigen Läufen, ohne etwas zu entscheiden. Berichtigt wurde nur die Angabe.

---

## B5 · Ausgabe-Wächter: Prüfagent fand ZWEI stille No-Ops in meinem eigenen Wächter

Beide waren genau die Fehlerklasse, gegen die der Wächter gebaut ist.

**HOCH 1 — die gesamte Queue-Plattenmessung war ein No-Op.** `df -P --output=avail .` wird von GNU
coreutils verweigert („die Optionen -P und --output schließen sich gegenseitig aus"), `2>/dev/null`
schluckte es. Selbst nachgestellt: mit `-P` kommt keine Zahl, ohne `-P` kommt `301259432`. Folge:
`frei_vor` war leer, die Statusdatei meldete immer „0 GB", und `[ "${frei_nach:-1}" -eq 0 ]` konnte
**nie** feuern, weil `:-` auch die leere Variable ersetzt. Die ausdrückliche 0-Byte-Warnung war
unerreichbar. `-P` gestrichen.

**HOCH 2 — die Bedarfsschranke unterschätzte den `CFD_VTK_JEDE`-Pfad.** `n_dump` zählte nur die
`CFD_VTK_DT`-Uhr. Bei `CFD_VTK_BEHALTE=0` löscht `vtk_rotiere` nichts, die Kadenz-Dumps bleiben also
liegen und waren ungezählt. Extremfall `CFD_VTK_JEDE>0, CFD_VTK_DT=0, CFD_VTK_ENDE=0, BEHALTE=0`:
Bedarf „0,0 GB" → Schranke lässt **jeden** Lauf durch. Falsche Entwarnung. Die Zahl (`kp`) lag drei
Zeilen weiter unten bereits vor; jetzt als `n_kadenz` vorgezogen und eingerechnet.

**MITTEL 3 — keine Reserve für Nicht-VTK-Ausgaben.** Gemessen: `p4_regel7` 188,4 MB in 95 Dateien,
`p4_regel6` 176,1 MB in 61 Dateien (alles außer `*.vtk`). Angesetzt 1,0 GB — gut das Fünffache des
Gemessenen, klein gegen einen Dump (15,3 GB). Deklarierter Wert mit Messgrundlage im Kommentar.

**MITTEL 4 — der VTK-Schreiber prüfte nur das Öffnen, nie das Schreiben.** Kosten im Schadenslauf:
`feld_nah_000150ms.vtk` 6,97 GB statt 11,13 GB, `feld_fern_000150ms.vtk` 0 B,
`feld_{nah,fern}_000300ms.vtk` 0 B — lautlos, und die Erfolgsmeldung wurde trotzdem gedruckt. Das
ist der **erste** Verlustzeitpunkt (16:43), sieben Minuten vor dem CSV-Verlust. Prüfung nach
`close()` eingebaut, mit Soll/Ist-Größenvergleich.

**MITTEL 5 — der Meldetext erreicht im Schadensfall das Log nicht.** `print_error` → `println` →
`std::cout`, und std::cout nimmt bei ENOSPC selbst badbit. Belegt: `logs/p4_regel6.log` steht bei
2 142 208 B mit mtime 16:43, während der Lauf bis 17:18 weiterlief — und erholte sich nicht, als
wieder Platz da war. **rc=1 ist das einzige Signal, das überlebt.** Das ist ein Argument *für*
`print_error` (die Queue macht aus rc „FEHLER") und *gegen* jedes Vertrauen in die Meldung. Als
Begründung im Code vermerkt, warum die Schranke VOR dem Lauf wichtiger ist als jede Meldung darin.

**MITTEL 6 — Abdeckung nur `fahrzeug_dd`.** Kugel (`fac_csv`) und Kanal (`kanal_zeit.csv`)
nachgezogen; vier bewachte Schreibstellen statt zwei.

**NIEDRIG 7** — toter `dauer`-Ausdruck und ein Kommentar, der noch die verworfene 10-%-Prüfung
ankündigte. Beides bereinigt; `t_vor`/`t_nach` tragen jetzt die Wanduhr in der Statuszeile.

### Wirknachweis des Wächters (eigener Test gegen /dev/full, g++ 15, libstdc++)

    mit std::flush, nach 1 Zeile : good=0 fail=1 bad=1   <- feuert an der ERSTEN verlorenen Zeile
    ohne flush                   : good() kippt erst nach 432 Zeilen (8640 B Puffer)
    nie geoeffneter Strom        : good=1                <- kein Fehlalarm
    open fehlgeschlagen          : good=0                <- richtiger Alarm

Alle vier bewachten Schreiber flushen zeilenweise. **Der Wächter hätte `p4_regel6` um 16:50
gestoppt** statt 28 Minuten weiterrechnen zu lassen; die Bedarfsschranke hätte ihn gar nicht erst
gestartet (3 × 15,7 GB = 47 GB Bedarf gegen ~7 GB frei).

**Was weiterhin NICHT getragen wird** und ehrlich dazugehört: die Beweiskette des Prüfagenten zeigt,
dass `forces.csv` in `p4_regel6` als letzte Zeile eine **zerrissene** Zeile trägt
(`0.20.20300001,-1682.5801,…`) — 4 Bytes gingen raus, der Rest blieb im filebuf und wurde erst bei
`close()` um 17:18 angehängt. Der Wächter greift an genau diesem Flush.

---

## B6 · §8.3 `boden_eq` × Rekonstruktion — entschieden mit einer Zahl: die Kollision ist LEER

Zensus über alle 3 275 271 Facettenzellen von `p4_regel7` (4 mm), `facetten_histogramme.csv`
Spalte `n` gegen den flags-Block von `feld_nah_001001ms.vtk` (die letzten N Bytes der Datei),
`boden_eq`-Parameter aus `logs/p4_regel7.log`: z = 1..2, ab Nase DOWN = 1, split-Voxel 113,
ABSTAND = 2. Werkzeug abgelegt als `werkzeuge/boden_eq_facetten_zensus.py`.

| `CFD_BODEN_EQ_ABSTAND` | von `boden_eq` überschriebene Facettenzellen |
|---|---|
| 0 | **2 910** (0,089 % aller Facetten), alle bei z = 1, x 342..987 |
| 1 | **0** |
| 2 (Standard) | **0** |

2910 Facettenzellen liegen überhaupt in der `boden_eq`-Zone, und **alle** werden von der
ABSTAND-Aussparung erfasst (`kernel.cpp:4335-4342`, Slot 117). Das ist nicht knapp: schon a = 1
deckt alles ab, weil eine Facettenzelle per Definition einen Solid-Nachbarn hat und der Scan
dz ∈ [0,a] × dx,dy ∈ [−a,a] ihn findet.

**Der Worst Case trifft die harmloseste Stelle:** die 2910 sitzen sämtlich bei z = 1 im
Radkontaktbereich. Bei 4 mm gilt `CFD_KRAFT_ZBAND = 4`, ihre Kraft wird also ohnehin ins **Band**
gebucht und aus `cd_rest`/`cz_rest` herausgerechnet.

**Folge:** kein Konstruktor-Wächter nötig. Was in den Code gehört, ist ein **Zensus zur Bauzeit**
statt einer Annahme — beim Facettenbau zählen, wieviele Facettenzellen in der `boden_eq`-Zone
liegen UND nicht ausgespart werden, `print_error` bei > 0 unter Rekonstruktion. Dann trägt die
Zahl sich selbst, auch wenn jemand ABSTAND senkt oder die Geometrie wechselt.

**Fassungsvorbehalt:** gemessen am 4-mm-Gitter mit `CFD_Y_VERSATZ=1`. 8 mm und 3,75 mm sind
ungemessen — genau deshalb gehört der Zensus in den Code und nicht in ein Dokument.

---

## B7 · Plan-Schritt 2 gebaut: `CFD_TIMER_APG`

Timer um den Vorkernel `fac_apg_ab` nach dem Muster `CFD_TIMER_FERN` (`setup.cpp`, `timer_fern`).
Statischer Schalter + **Instanzkopie** `timer_apg` neben `apg_haken` (`lbm.cpp:873`) — dieselbe
Falle wie bei `apg_on`: `fahrzeug_dd` nullt die Statik vor dem Bau des Fernfelds (Prüfbefund
HOCH-1 vom 16.09.). Aus-Pfad ohne `finish_queue`, ohne `Clock`, `kernel.cpp` unangetastet.
Drei Wächter: Wertebereich, `CFD_TIMER_APG=1` ohne `CFD_FAC_APG` → `print_error` (stiller No-Op),
`apg_t_n == 0` am Laufende → `print_error`. Dazu die ausdrückliche Ansage, dass der Arm
serialisiert und seine Wanduhr kein Leistungsmaß ist — die Lehre, die `CFD_TIMER_FERN` am 21.09.
einen ganzen Lauf gekostet hat. `scratch_gate` Exit 0. Prüfagent läuft.

---

## B8 · Prüfagent zu `CFD_TIMER_APG` — ein PRODUKTIONSDEFEKT in meinem eigenen Commit

**HOCH 1, und es ist der schlimmste Fehler des Tages.** Mein Kommentar an `lbm.cpp:883` stand
**mitten in der Zeile**, vor der nächsten Zuweisung. Damit lag

    nb_stride = apg_on ? 5ull : 2ull;

vollständig im `//`-Kommentar. `nb_stride` blieb auf dem Header-Default `2ull`, während der JIT
weiter `def_nb_stride 5ul` emittiert (`lbm.cpp:2406`, der liest die **Statik**). `fac_nb` wäre mit
`2*aktiv` Floats entstanden, die Kernel schreiben und lesen aber bei `5*gid`
(`kernel.cpp:5621`, `:5578`, `:2075`) — **ab gid ≥ 0,4·aktiv jeder Zugriff außerhalb des Puffers,
2,5-facher Überlauf ohne Schranke auf der GPU.** Das ist die Wedge-Klasse auf der B70.

Drei Dinge daran sind bitter:
1. Es traf **auch den Timer-AUS-Pfad**, also **jeden APG-Lauf ab `c5bea1d`** — meine Zusage
   „Aus = bitgleich" war durch die Zeile daneben verletzt, nicht durch die Timer-Logik.
2. Es ist **genau die Falle, die im Gedächtnis steht** („`//` mitten in einer Zeile frisst Code",
   V2-Werkzeugfallen, dreimal bezahlt) — und der verschluckte Kommentar beschreibt sie selbst.
3. Es kompiliert lautlos; der Produktionsbuild läuft ohnehin mit `-w`.

Behoben: jede Zuweisung auf einer **eigenen** Zeile, mit der Begründung daneben. Dazu den
**Stride-Wächter vorgezogen** — der vorhandene (`setup.cpp`, `berichte_apg`: `st!=5ull`) feuert erst
am Laufende und ist ein Nachruf; jetzt steht vor dem Pufferbau (`lbm.cpp:1724`) ein `print_error`,
wenn `nb_stride != (apg_on ? 5 : 2)`.

**HOCH 2 — mein Wächter tötete `fahrzeug_dd`.** Der Konstruktor-`print_error` bei
„`CFD_TIMER_APG=1` ohne `CFD_FAC_APG`" feuert beim Bau des **Fernfelds**: `setup.cpp:7106` setzt
`s_fac_apg`, `:7177` baut `lbm_f`, `:7198` **nullt die Statik**, `:7449` baut `lbm_c` — und der
Fernfeld-Konstruktor sieht `s_fac_apg==0`. Damit wäre der einzige Fall, der die +17,0 % erzeugt,
vor dem ersten Zeitschritt gestorben. Derselbe Fehler, den `setup.cpp:7109` für
`CFD_FAC_APG_HAKEN` am 16.09. schon einmal behoben hat — die Lehre steht zwei Zeilen darüber.
Behoben: Ansage-Prüfung an alle drei Fallstellen (Kanal, Kugel, dd) verlegt, im Konstruktor bleibt
nur die Wertebereichsprüfung.

**MITTEL 3 — Bericht nur im dd-Fall.** In Kanal und Kugel hätte der Timer jeden Schritt
serialisiert und **nie eine Zahl gedruckt** — die Klasse „Schalter ohne feuernden Zähler".
Behoben: gemeinsamer Helfer `berichte_apg_zeit()`, aufgerufen aus dd, Kanal und Kugel.

**MITTEL 4+5 — die Prozentzahl war in keine Richtung eine Schranke, und ich hatte sie als
untere Schranke verkauft.** Die Queue ist **in-order** (`opencl.hpp`, kein
`CL_QUEUE_OUT_OF_ORDER_EXEC_MODE_ENABLE`) — Kernel derselben Queue überlappen nie, die Pipeline
verdeckt also gar keine Ausführungszeit. Der **Zähler** enthält zusätzlich Enqueue, Submission und
das Aufwachen aus `clFinish`, überschätzt den Kernel also; der **Nenner** trug Serialisierung *und*
den VTK-Enddump. Zwei gleichgerichtet nach oben verzerrte Größen → der Quotient schrankt nichts ein.
Behoben: **Prozentwert ersatzlos gestrichen**, es stehen absolute ms/Spanne/Summe. Als besserer Weg
notiert: OpenCL-Event-Profiling (`enqueue_run` nimmt schon einen Event, es fehlt nur
`CL_QUEUE_PROFILING_ENABLE`) — dann bliebe der Arm sogar ein Leistungsmaß.

**MITTEL 6 — Warnung reicht nicht.** `CFD_TIMER_FERN` warnt seit dem 16.09. und stand am 21.09.
trotzdem in einer Produktionszeile. Eine Queue, die nachts läuft, liest keine Warnungen. Eingebaut:
harte **Namenssperre** in `lauf_queue.sh` nach dem Muster der Atomik-Sperre (mit `\+?0*[1-9]`,
`[[:space:]]`-Anker und geerbter Umgebung — die drei Lehren aus Befund B7/M4 vom 16.09.): ein
`CFD_TIMER_*`-Arm startet nur, wenn der **Laufname** `timer` enthält. Kein Verbot, ein Namenszwang
— dann heißt auch jede Logdatei so.

**NIEDRIG 7** — `apg_t_max` trägt die `t%zaehl_takt`-Atomics des Vorkernels und ist kein Jittermaß;
steht jetzt im Warntext.

**Eigene Lehre des Tages, die ich mir merken muss:** ich habe zweimal hintereinander ein
`echo "Build ok"` gedruckt, das **unabhängig vom Compilerstatus** lief — einmal nachdem der Build
mit vier Fehlern abgebrochen war. Ein Statuszeichen, das nicht am Status hängt, ist genau derselbe
stille No-Op, den dieses Projekt jagt. Seitdem: `if make ... | grep -qE '\berror\b'; then …`

---

## B9 · CPU-Sprosse: die heutigen Änderungen im Binary belegt

`t22_timer_apg_cpu` (Kanal N=20, Facetten 3, ELIBB+NACHBAR+APG, `CFD_TIMER_APG=1`, Gerät 0 = CPU,
über die Queue, rc=0, 0 Fehler, 1 min). Vier Nachweise:

1. **`nb_stride` trägt wieder 5.** Der APG-Abschlussbericht (`berichte_apg`) prüft `st!=5ull` und
   hat **nicht** gewarnt, rc=0 — der Fix des verschluckten Kommentars ist damit im Binary belegt,
   nicht nur im Diff. Der neue Wächter vor dem Pufferbau feuerte nicht falsch.
2. **`[APG-ZEIT] Kanal`** erscheint — Prüfagent-Befund 3 (Bericht nur im dd-Fall) ist behoben.
   Zahl: **Mittel 0,070 ms über 266 666 feine Schritte, Spanne 0,057 .. 0,643 ms.**
3. **Die Queue-Plattenzeile liefert echte Zahlen** („frei 287 GB", „287 → 287 GB"), nicht mehr
   „0 GB" — der `df -P`-No-Op ist weg.
4. **Die laute Gatterung spricht:** „keine Kraftbilder — cd_facetten.csv fehlt oder ist leer".
   Im Kanalfall ist das richtig und war vorher stilles Nichts.

Und am 8-mm-Lauf, live: **`PLATTENBEDARF: 3 Dump(s) gleichzeitig à 1952 MB + 1,0 GB Reserve für
CSV/PNG/Berichte = 6,7 GB`** — die Bedarfsschranke rechnet im Binary, mit der Rotationsspitze
`behalte+1` und der gemessenen Reserve.

---

## B10 · §8.4 `fac_tau_cnt`-Politik beantwortet

**Ja, die Rekonstruktion muss den Zähler erhöhen — und der Akkumulator muss geteilt werden.**
`fac_tau_acc[6·fid+0] += tw` und `[+1..3] += fw` stehen in einer Zeile (`kernel.cpp:2759`),
`fac_tau_cnt[fid] += 1u` folgt bei `:2792`. Vier Verbraucher hängen daran:

| Verbraucher | Stelle | ohne Zählung |
|---|---|---|
| `tw = acc[0]/cnt` → y⁺ → van-Driest-D² auf nu_t | `kernel.cpp:3469-3472` | `vd_cnt==0` ⇒ **ganzer VD-Block übersprungen**, nu_t an Lage 1 ungedämpft |
| Warmstart der u_s-/P-Filter (`cnt==0`) | `kernel.cpp:2255` | re-seedet jeden Schritt, kein Filteraufbau |
| Host-y⁺-Spiegel `fac_tau[6i]/fac_tau_n[i]` | `setup.cpp:620-626` | y⁺-Reihe tot |
| Reibungsanteil `K.rx/ry/rz` aus `acc[1..3]` | `setup.cpp:4324-4326` | `cd_reib`/`cz_reib` verlieren die Quelle |

Kernpunkt: `acc[0]`/`cnt` laufen weiter (die Rekonstruktion rechnet das Spalding-Ziel ohnehin),
aber `acc[1..3]` darf **nicht das Ziel** bekommen, sondern das **gemessene ΔP** — sonst wird der
Ist=Soll-Detektor tautologisch. Und das ΔP ist der *gesamte* Impulsübertrag: ungeteilt gebucht
verschiebt die Rekonstruktion **Druck in die Reibung**. Die Aufteilung braucht das ΔP·n̂-Histogramm
aus §4, nicht die Annahme „u_n ≈ 0 an der Wand".

---

## B11 · 8-mm-A/B `CFD_FAC_KRAFT=2` — der Arm KIPPT (Heiko-Freigabe 22.09. für 8-mm-Läufe)

Serie `logs/serie_k2_8mm.txt`, Basiszeile frisch aus `werkzeuge/basis_zeile.py 8`
(trägt `CFD_KRAFT_ZBAND=3` nach der korrigierten Bandregel), gepaart, identisches Binär,
**eine Variable**, Gerät 1 (B70), Census vor und nach der Serie frei.

| Arm | Ergebnis |
|---|---|
| `k2_basis_8` | rc=0, 6 min Wanduhr, 4 Kraftbilder |
| `k2_kraft2_8` | **rc=1 — gekippt bei t = 0,03307 s (grober Schritt 248): Fx = NaN, Fz = NaN** |

**Der Arm ist am 8-mm-Fahrzeug mit dem heutigen Stand nicht lauffähig.** Er stirbt weit vor dem
Warmlaufende (0,201 s), es gibt also **keine Kraftaussage** aus ihm.

Was sich bis zum Kippen zeigt — **ausdrücklich Anwärmphase, keine belastbare Zahl** (Lehre vom
21.09.: „eine Zahl aus der Anwärmphase als belastbar ausgegeben" kostete einen falsch begründeten
Commit). 30 gemeinsame Abtastungen, `forces.csv` beider Arme:

    t [s]     Fx Basis     Fx K2     Fz Basis     Fz K2
    0.001067   18823.8    29777.0     -2924.5    -2690.0
    0.009600    6924.8     9459.1     -4378.9    -3992.1
    0.018133   11177.1    12106.4     -2537.9    -3185.8
    0.026667    4925.3     7215.0      -250.4    -1861.9
    0.032000    6684      8863 (+32,6 %)

Die **Richtung** deckt sich mit dem gemessenen Negativ von Stufe 1 (30.08.: `cd_druck_rest`
+21,9 %): die exakte Durchsetzung des Wandmodellziels erhöht den Widerstand. Mehr ist daraus nicht
zu lesen.

### Verdacht auf die Kippursache — und er steht seit gestern im Plan

`CFD_POSITIV=2` nimmt **Facettenzellen der Klasse K0 aus** (`kernel.cpp:4012`, Begründung
„Wandschub-Weitergabe", Entscheid E2). `CFD_FAC_KRAFT=2` setzt das Wandmodellziel an **allen**
Facettenzellen exakt durch und kann dort `f_i < 0` erzeugen — an genau den Zellen, die der
Positivitätsbegrenzer nicht sieht. `REKONSTRUKTION-PLAN.md` §6 nennt das wörtlich als offenen
Punkt für die Rekonstruktion: *„Die Rekonstruktion kann f_i < 0 erzeugen → Zähler und Entscheid,
ob `CFD_POSITIV_FACETTE=1` Pflicht wird."*

`CFD_POSITIV_FACETTE=1` schließt K0 ein (`lbm.cpp:626`). Nächste Serie (`serie_k2b_8mm.txt`),
zwei Arme, damit die Kraftaussage **eine** Variable behält:

    k2_pf_8        = Basis + POSITIV_FACETTE=1            (Kontrolle: bewegt der Begrenzer allein?)
    k2_kraft2_pf_8 = Basis + POSITIV_FACETTE=1 + KRAFT=2  (gegen k2_pf_8: eine Variable = KRAFT)

Damit ist entweder die Obergrenzen-Messung da, oder §6 ist mit einer Messung beantwortet — die
Rekonstruktion hat dieselbe Fehlermöglichkeit.

---

## B12 · Serie 2: `CFD_POSITIV_FACETTE=1` ist kraftneutral und verschiebt das Kippen um Faktor 8

Serie `logs/serie_k2b_8mm.txt`, dieselbe Basiszeile, identisches Binär, Gerät 1, Census frei.

| Arm | Ergebnis |
|---|---|
| `k2_pf_8` (Basis + PF=1) | rc=0, 6 min, 280 Abtastungen ab 0,201 s |
| `k2_kraft2_pf_8` (+ KRAFT=2) | **rc=1 — gekippt bei t = 0,26880 s** (grober Schritt 2016), 62 Abtastungen ab 0,201 s |

### Befund 1 — der Verdacht aus Plan §6 trägt, heilt aber nicht

Ohne `POSITIV_FACETTE` kippte `KRAFT=2` bei **t = 0,033 s**, mit ihm bei **t = 0,269 s** —
**Faktor 8,1 später**. Die K0-Ausnahme des Positivitätsbegrenzers (`kernel.cpp:4012`) ist damit
als *mitwirkende* Ursache belegt, aber sie ist nicht die einzige: der Arm stirbt trotzdem.

### Befund 2 — `CFD_POSITIV_FACETTE=1` kostet in den Kräften NICHTS

`k2_basis_8` gegen `k2_pf_8`, 280 gepaarte Abtastungen ab 0,201 s, r = 0,984:

| Größe | Basis | + PF=1 | Δ ± SEM | σ | Urteil |
|---|---|---|---|---|---|
| `cd_druck_rest` | 0,4447 | 0,4412 | −0,0035 ± 0,0019 | 1,8 | nicht von Rauschen zu trennen |
| `cz_druck_rest` | −0,7307 | −0,7285 | +0,0021 ± 0,0068 | 0,31 | nicht von Rauschen zu trennen |

**Das beantwortet Plan §6 mit einer Messung:** K0 in den Begrenzer einzuschließen ist
kraftneutral und verschiebt die Divergenzgrenze um Faktor 8. Unter Rekonstruktion, die dieselbe
Fehlermöglichkeit hat, sollte `CFD_POSITIV_FACETTE=1` **Pflicht** sein — Preis null, Nutzen belegt.

### Befund 3 — die Obergrenze, um die es ging, ist ein NEGATIV

`k2_pf_8` gegen `k2_kraft2_pf_8`, 62 gepaarte Abtastungen im Fenster 0,201–0,269 s, r = 0,798:

| Größe | PF=1 | + KRAFT=2 | Δ ± SEM | σ | Urteil |
|---|---|---|---|---|---|
| `cd_druck_rest` | 0,4412 | 0,5366 | **+0,0670 ± 0,0116** | 5,76 | unterscheidbar, **+21,6 %** |
| `cz_druck_rest` | −0,7285 | −0,6760 | +0,0941 ± 0,0342 | 2,75 | s. Vorbehalt |

Die exakte Durchsetzung des Wandmodellziels an **allen** Facettenzellen
- **erhöht den Druckwiderstand um 21,6 %** — die Größe, die ohnehin nahe der Referenz liegt,
- **verringert den Abtrieb** (|Cz| −7,2 %) — die Größe, die der ganze Plan schließen will,
- und ist **numerisch instabil**.

Die Richtung deckt sich quantitativ mit Stufe 1 vom 30.08. (`cd_druck_rest` +21,9 %). Zwei
unabhängige Arme, zwei Fassungsstände, dieselbe Aussage.

### DREI VORBEHALTE, und sie sind keine Formalität

1. **Der Lauf divergiert.** Die 62 Abtastungen stammen aus einer Lösung, die 68 ms später auf NaN
   läuft. Werte aus einem kippenden Lauf sind konstruktiv verdächtig — das ist die schwächste
   Stelle dieser Messung und darf nicht weggelassen werden.
2. **Das Fenster ist 68 ms lang.** Der Speicher sagt selbst: „die Differenzreihe vergisst sich
   erst nach ~4 Abtastungen, ein Block ist nur 7 lang — der Fehlerbalken ist zu klein, das sigma
   zu groß." Für `cz` bleiben **7,2 unabhängige Beobachtungen**. Die Cd-Aussage (16,9 unabhängige
   Beobachtungen, 5,76 σ) trägt deutlich besser als die Cz-Aussage.
3. **Die Mindestlauflänge ist gerissen.** `BOX-REGELWERK.md` verlangt seit dem 21.09. 5 ×
   Fahrzeuglänge = 739 ms; hier sind es 269 ms bis zum Abbruch.

**Trotz aller drei:** für ein *Negativ* reicht es. Ein Mechanismus, der die Zielgröße in die
falsche Richtung bewegt **und** den Löser umbringt, muss nicht auf 501 ms nachgewiesen werden.

---

## B13 · Prüfrunde 2: Hauptdefekt bestätigt behoben, drei Nachzügler geschlossen

Der unabhängige Prüfagent bestätigt gegen `c5bea1d..a99be5a`:
- `nb_stride` steht wieder als Code, der Zuweisungssatz ist identisch zum funktionierenden
  16.09.-Stand plus `timer_apg` — **es fehlt nichts**.
- **Kein zweiter Fall derselben Klasse im Baum.** Alle `src/*.cpp`/`*.hpp` mit String-Maskierung
  gescannt; jeder Treffer ist Prosa oder absichtlich stillgelegter Upstream-Code. Zwei Zusatzproben:
  kein `//` innerhalb eines String-Literals, und `//` in `R()`-Blöcken ist **ungefährlich**
  (das `R`-Makro stringifiziert in Phase 4, Kommentare verschwinden schon in Phase 3 — mit `g++ -E`
  nachgestellt). **Die Gedächtnisnotiz „`//` frisst Code in R()-Blöcken" ist damit präzisiert:**
  die echte R()-Falle ist `#define`/`#if` im Block, nicht der Kommentar.
- Shell-Sperre: **vierzehn nachgestellte Fälle** bestanden (`=1`/`=01`/`=+1`/`=2`/`=10` verweigert,
  `=0`/`=00`/`=+0` durch, TAB-getrennt verweigert, geerbt und überschrieben korrekt, Platzierung vor
  dem Start).

**Drei Nachzügler behoben:**
- **B3** — `"t%%zaehl_takt"` stand in einem gewöhnlichen `std::string`; `print_warning` formatiert
  nicht, im Log hätte wörtlich `%%` gestanden.
- **B2** — der Warntext sagte „ein zusätzliches `finish_queue`", der Code setzt **zwei**
  (`lbm.cpp`). Untertrieb die Serialisierung um Faktor 2; jetzt richtig, mit dem Hinweis, dass das
  hintere im Single-GPU-Fall teilredundant ist.
- **B1 (MITTEL)** — `CFD_TIMER_APG` wurde in `fahrzeug`, `fernfeld` und `facetten_test` **nirgends**
  angesagt, während `lbm.cpp` trotzdem „DIAGNOSEARM" druckte: der Leser bekam eine Bestätigung für
  einen No-Op.

### Und hier der zweite eigene Fehler desselben Musters, vom Test gefangen

Mein erster B1-Fix stand **am Ende von `main_setup()`** — unerreichbar: `main_setup_facetten_test`
endet mit `_exit(0)`, `main_setup_fernfeld` ebenso. Der CPU-Nachweis kam mit **err=0** zurück, der
Wächter schwieg also genau in dem Fall, für den ich ihn gebaut hatte. Ein Wächter, der nicht
feuert, ist derselbe stille No-Op, den er verhindern soll — zum zweiten Mal an einem Tag.

Jetzt steht die Ansage **vor** der Fallauswahl, damit in jedem Fall erreichbar und mit Abbruch
**vor** dem Lauf statt danach. Nachweis am CPU-Paar (`logs/serie_t22b_cpu.txt`):

    t22b_timer_ja_cpu    (kanal + APG + TIMER_APG=1)     rc=0, err=0   <- schweigt, richtig
    t22b_timer_nein_cpu  (facetten_test + TIMER_APG=1)   rc=1, err=1   <- feuert, richtig
      ! CFD_TIMER_APG=1 mit CFD_CASE=facetten_test: dieser Fall fuehrt kein APG ...

**Lehre für mich, zweimal an einem Tag bezahlt:** eine Ansage ist erst gebaut, wenn sie in einem
Lauf GEFEUERT hat. „Compiliert und steht an plausibler Stelle" ist kein Nachweis.

Offen und bewusst stehengelassen: B4 (`grep -q 'timer'` ist case-sensitiv, Richtung sicher),
B5 (der Stride-Wächter prüft Member gegen Member, nicht gegen die JIT-Statik — er fängt die
heutige Fehlerklasse, nicht jede Divergenz), B6 (Kugel heißt im Log einmal „Gitter", einmal
„Kugel"), B7 (Altlast: `berichte_apg_zeit` liest nur Domäne 0, wie `berichte_apg` seit je).

---

## B14 · Heiko-Auftrag: Wandzellen am Dach, die zurückfallen — DIAGNOSE

Heiko (remote): „durchsuche/diagnostiziere bitte die Wandzellen die dort am Dach zurückfallen …
wir hatten schon einmal einen VTK-Export gemacht, bei dem ich sehen konnte welchen Status die
echten Wandzellen am Dach haben und da war einiges Rang 0 und 1."

**Die gesuchte Datei ist `facetten_guete.vtk`** (`setup.cpp:4145ff`, „GUETEKLASSE JE WANDZELLE,
VTK für ParaView, Heiko 29.08."), Schalter `CFD_FACETTEN_VTK=1` (`setup.cpp:3304`), heute aus.
Kategorien: 0 = kein Wandmodell (reines BB), 1 = Normale unsicher, 2 = Normale grob,
3 = y_w geklemmt, 4 = voll. Dazu `zensus_facetten.vtk` (`CFD_FAC_ZENSUS_VTK=1`, `setup.cpp:3087`)
mit `n_wandlinks` je Facette. Diagnoselauf `diag_dach_8` erzeugt beide.

### Zwischenbefund aus vorhandenen Daten — das Dach ist NICHT die schlechte Region

Dachdefinition ohne Normalen, damit eindeutig: je (x,y)-Säule die **oberste** Layer-1-Facettenzelle,
davon die oberen 40 % der Höhen ⇒ 48 018 Zellen, x 256..614 (Fahrzeug x 58..614), z 121..152. Das
trifft den Übergang Frontscheibe→Dach und alles dahinter.

| | Dach (48 018) | alle Layer 1 (619 700) |
|---|---|---|
| y_w Median | 0,500 (echte Wandzellen) | 0,500 |
| `eigene_links ≤ 1` — **Rang 0 garantiert** | **28 = 0,06 %** | 4 961 = 0,80 % |
| `eigene_links == 2` — Rang 0/1 möglich | 396 = 0,82 % | 5 930 = 0,96 % |
| `klasse != 0` — kein Wandmodell | **58 = 0,12 %** | 37 067 = 5,98 % |
| ≥ 3 Links | **99,1 %** | — |

**Das Dach ist die sauberste Region des Fahrzeugs**, nicht die schlechteste. Die geometrische
Verwurfsquote nach Höhe: z 0–40 (Unterboden/Rad) 4,93 %, z 40–80 (Mitte) **11,91 %**,
z 80–120 3,68 %, z 120–152 (Dach) **1,25 %**.

**Folge: wenn am Dach die Abdeckung/Rückfallquote leidet, ist der RANG nicht die Ursache.**
Die Rückfallquote liegt bei 24,15 % (heute gemessen, `slots_verlauf.csv`), Rang-0-Besuche machen
davon nur 2,585 % aus. Der Rest muss aus anderen Quellen kommen — Hauptverdacht ist das
Sättigungsgate: `README.md` (16.09.) sagt, von den 28,8 % nicht erreichten Zellen sei der
**gesamte** Betrag das Gate, „a model decision, not a geometry hole"; Abschalten senkt die
Rückfallquote auf 18,9 %, verschiebt die Kräfte aber um 16,9 σ von der Referenz weg.

**Offen und als Nächstes:** Güteklassen 1/2/3 am Dach aus `facetten_guete.vtk` (die könnten im
Bild wie „Rang 0/1" ausgesehen haben), und die zonenweise Aufschlüsselung der Rückfall*gründe*
— die vorhandenen Slots sind global, das braucht eine Zonenzählung im Code.

---

## B15 · APG kann in dieser Parametrierung nicht wirken — die Klemme rechnet, nicht das Modell

Heiko fragte, ob APG überhaupt richtig wirken kann. Antwort aus `logs/k3_pf_rang_8.log` (8 mm, heute,
κ = 1,0, Slots 313..316 erstmals ausgelesen):

| Autorität \|κ·y_ab·dp/ds\| / τ_w | < 0,1 | 0,1–0,5 | 0,5–1 | **≥ 1** |
|---|---|---|---|---|
| Anteil | 2,0 % | 0,4 % | 0,5 % | **97,1 %** |

n = 36 596 164. Und die Folge: **Klemme unten [309] 17 181 509, oben [310] 18 338 776 = 97,06 %**
aller APG-Besuche. dp/ds > 0 in 17,4 Mio, < 0 in 18,5 Mio Besuchen — bei Gegendruck wird das Ziel
auf **0** geklemmt, bei beschleunigter Strömung auf **2·τ_w**.

**APG ist damit ein Vorzeichendetektor, keine Druckgradientenkorrektur.** Das erklärt den gemessenen
Nulleffekt (PLAN-APG §I: −0,0022 ± 0,0011, 41 Vorzeichenwechsel) vollständig: die beiden Klemmen
heben sich im Mittel auf.

**Nicht falsch codiert, nicht falsch konfiguriert.** Die drei Baufehler aus `PLAN-APG-2026-09-16.md`
§A sind alle behoben: A1 (`yw_ab` statt `yw`, `kernel.cpp:2136`), A2 (∇ρ als zentrale Differenz über
6 Achsnachbarn im Vorkernel, kein 1-Parameter-Fit), A3 (`load_f(t+1)`, deterministisch). κ = 1,0 ist
der **herleitbare** Wert (A7: 0,5 ist ein widerrufener Interim). Das Modell wird schlicht außerhalb
seines Gültigkeitsbereichs betrieben: die Dünnschichtbilanz setzt voraus, dass y_ab·dp/ds **klein**
gegen τ(y_ab) ist — bei 8 mm ist y_ab ~1,5 Zellen und τ_w winzig.

**Folge: ein harter Klemmbereich ist der falsche Begrenzer.** Mozaffaris echte Form
f(α_p) = 1 − C·α_p/(α_p+α_p0) mit α_p = ν·(dp/ds)/(ρ u_τ³) sättigt **von selbst** bei 1 − C = 0,6
und braucht keine Klemme. Sie existiert in V1 (`FluidX3D/src/kernel.cpp:2153-2169`), die gesamte
Infrastruktur (∇ρ deterministisch, y_ab, u_τ, ν, Autoritätshistogramm) steht in v2 bereits.
**Es ist ein Formeltausch, kein Neubau.** Ehrliche Erwartung aus V1: −0,87 % Cd bzw. +0,03 % ± 8,2 %,
also Nulleffekt — der Gewinn wäre erstmals ein *belegbar arbeitender* Mechanismus, nicht mehr Kraft.

---

## B16 · SGS-Überviskosität ist die Ursache, und die Richtung ist gemessen

Drei Arme, 8 mm, dieselbe Basiszeile, je eine Variable. **Ausgewertet wird H = δ*/θ, nicht δ₉₀**
(Befund B76 vom 03.09.: mehr Wandschub macht die Grenzschicht dicker, aber voller — δ₉₀ zeigt in die
falsche Richtung). Kette: `fx_band.py` → `fx_profil2.py`, je 3 Zeitpunkte × 10 y-Ebenen = 30 Proben.

| Zone | H `k2_basis_8` (SISM=1) | H `h8_sism0` | Δ | OF13 |
|---|---|---|---|---|
| Saugspitze 2,00–2,30 | 1,859 | 1,985 | +0,126 | — |
| **Dachplateau 2,30–2,90** | **2,004** | **2,253** | **+0,249** | **1,174** |
| δ₉₀ Dachplateau | 52 mm | 68 mm | +16 mm | — |

**Wirkrichtung im Code nachgeschlagen** (`kernel.cpp:5512`):
`fac_wfd = 1/(tau0 + 3·0,030021·max(0, |S|_FD − Sbar))` — der SISM-Abzug **verkleinert** den Nenner,
also **weniger** ν_t. `SISM=0` bedeutet **mehr** ν_t. Damit: **mehr ν_t ⇒ H schlechter, weniger ν_t
⇒ H besser.** Die SGS-Überviskosität ist als Ursache bestätigt, und der Hebel zeigt nach unten.

SISM nullt ν_t heute schon an **34,3 %** der Besuche (Slot 127 = 8 871 811 von Slot 126 = 25 843 428)
und holt damit 0,249 von der Lücke 2,004 → 1,174, also **rund 30 %** des Wegs. Der Abzug ist aber an
`max(0, |S|−Sbar)` gedeckelt; `CFD_SGS_NUT_SKAL` skaliert ohne Deckel.

**Zwei Vorbehalte:** die Zone Frontscheibe (1,70–1,90) lieferte **null gültige Proben** — die
3,75-mm-Aussage „schon an der Frontscheibe zu dick" ist mit diesen Läufen **nicht** bestätigt. Und
die Heckscheibe (H 9,06 → 3,54) liegt im Ablösegebiet, wo H keine sinnvolle Kennzahl ist.

**8 mm ist keine Cd/Cz-Aussage** (Heiko, stehende Regel) — H und δ₉₀ sind Feldgrößen, die Kräfte
dieser Arme werden nicht als Befund geführt.

### Zwei Wächter haben mir heute je einen Arm gerettet

1. **Basis-Wächter:** erster `h8_sism0`-Versuch verweigert — „CFD_SGS_SISM: Soll 1, Ist 0,
   0 Abweichungen deklariert". Ich hatte `CFD_BASIS_ABWEICHUNG` vergessen. **0 min Kartenzeit.**
2. **Zwei-Senker-Wächter:** erster NUT_SKAL-Arm verweigert — „CFD_SGS_NUT_SKAL und CFD_SGS_SISM
   gleichzeitig: ZWEI nu_t-Senker an derselben Zelle, also zwei Variablen in einem Lauf
   (Iron Rule 1). Der Diskriminator misst gerade GEGEN SISM — er gehört in den Arm OHNE SISM."
   Ich hatte NUT_SKAL auf SISM=1 gelegt, obwohl `PLAN-APG` §0.3 genau das vorschreibt, was der
   Wächter sagt. **0 min Kartenzeit.**

Beide Male hätte der Arm gerechnet und wäre hinterher unbrauchbar gewesen. Das ist der Gegenbeweis
zu meiner eigenen Lehre von heute früh: Wächter, die VOR dem Lauf abbrechen, sind die billigsten.

---

## B17 · Die ν_t-Achse ist AUSGEMESSEN — und die Höhe ist kein Hebel, die Selektivität ist es

Vier 8-mm-Arme, dieselbe Basiszeile, je eine Variable, H = δ*/θ aus `fx_band.py` → `fx_profil2.py`
(je 3 Zeitpunkte × 10 y-Ebenen = 30 Proben). **8 mm ist keine Cd/Cz-Aussage** (Heiko, stehend).

| Arm | H Saugspitze 2,00–2,30 | H Dachplateau 2,30–2,90 | δ₉₀ Dach |
|---|---|---|---|
| `k2_basis_8` SISM=1 (**selektiver** Abzug) | 1,859 | **2,004** | 52 mm |
| `h8_sism0` SISM=0, ν_t × 1,000 | 1,985 | **2,253** | 68 mm |
| `h8_ns05` SISM=0, ν_t × 0,500 | 2,161 | 2,228 | 60 mm |
| `h8_ns148` SISM=0, ν_t × 0,148 | 2,147 | 2,202 | 68 mm |
| OF13 | — | **1,174** | — |

**Der Befund:** ν_t **gleichförmig** um 85 % zu senken (Faktor 0,148) bewegt H am Dach um **−0,051**.
Derselbe Dämpfungsbetrag **selektiv** über `max(0, |S|_FD − Sbar)` angewandt bringt **−0,249** —
**Faktor 4,9**. Die Höhe von ν_t ist kein Hebel; die **Selektivität** ist der Wirkmechanismus.

Der Wirkpfad war in beiden NUT_SKAL-Armen belegt: **Slot 188 = Slot 76 = 36 611 523**, „der Skalierer
erreicht die Facettenzellen vollständig und ausschließlich". Kein No-Op.

**Damit ist die Frage beantwortet, für die der Diskriminator laut `kernel.cpp:3493` gebaut wurde:**
*„ist der gemessene SISM-Kraftgewinn Modellphysik — oder nur die fehlende Wanddämpfung?"* →
**Modellphysik.** Eine bloße Dämpfung reproduziert SISM nicht einmal zu einem Fünftel.

**Zusatzbefund:** an der Saugspitze wird H mit beiden NUT_SKAL-Armen **schlechter** (1,985 → 2,161 /
2,147). Gleichförmige Dämpfung verschlechtert dort, wo die Strömung beschleunigt — genau das Regime,
in dem eine Hintergrundscherungs-Subtraktion richtig unterscheidet und eine pauschale Skalierung nicht.

**Folge für den Weg:** SISM holt 0,249 von der Lücke 2,004 → 1,174 (also 0,830), das sind **30 %**.
Die restlichen **70 % liegen nicht auf der ν_t-Achse**, sondern bei Auflösung und
Voxeltreppen-Rauheit (k/δ 13–20 % gegen 1–3 % im Windkanal, Befund B76) — und keiner der beiden
steht auf einem Schalter. Verbleibender Stellhebel auf dieser Achse: die Selektivität selbst
(`CFD_SGS_SISM_T` = 5000, `CFD_SGS_SISM_AB` = 15000, Form des Abzugs). Feinschliff, kein Durchbruch.

### Mein Fehler in dieser Reihe, und er war vermeidbar

Ich habe `CFD_SGS_NUT_SKAL=0.5` als „deklarierte Sonde" gesetzt. `kernel.cpp:3499-3501` sagt aber
ausdrücklich: **„DER FAKTOR IST KEIN STELLKNOPF, SONDERN EIN MESSWERT — er wird aus dem SISM-Lauf
ABGELESEN (1 − 0,852 = 0,148 für Lage 1)."** Der herzuleitende Wert stand also bereits im Code, und
ich habe daneben geraten. Das kostete einen Lauf (6 min) und hätte die Reihe fast falsch gedeutet:
mit nur dem 0,5-Punkt sah es nach „Hebel zu schwach" aus statt nach „falsche Wirkform".
**Lehre: bevor ich einen Schalterwert setze, den Kommentar am Schalter lesen — dort steht oft, dass
er abzulesen und nicht zu wählen ist.**

**Fassungsvorbehalt:** 0,148 stammt aus einer 4-mm-Lagenmessung vom 08.09.; der heutige 8-mm-Wert
ist ungemessen (die ν_t/ν₀-Dekadenslots 30..39 hängen an `CFD_SGS_DIAG`, hier aus). Zone
Frontscheibe (1,70–1,90) lieferte in allen vier Armen **null gültige Proben**.
