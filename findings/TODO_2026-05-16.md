# TODO — Next Session 2026-05-16

## Sofort-Aufgaben (Priorität HOCH)

### 1. Phase 3 Multi-Cell WALL_VISC_BOOST testen (READY IN CODE)
- Source ist drin (`lbm.cpp` populate_wall_adj_flag multi-pass; `kernel.cpp` Layer-skaliertes nu_t)
- Build verifiziert ok (compiles), aber **NOCH NICHT GETESTET**
- Tier-1 Quick-Test (5-20 chunks): verify no pathology, expected layer counts in print_info output
- Tier-2 Production-Run (~150 chunks): expected stärkerer effect als Phase 2 (3x mehr cells affected, deeper into flow)
- ParaView Sichtung: User-Quote "noch nicht stark oder tief genug" sollte mit Phase 3 verbessert sein
- Watcher mit auto-VTK-fix + ParaView-launch nutzen

**Command:**
```bash
cd /home/heiko/CFD/FluidX3D
nohup stdbuf -oL ./bin/FluidX3D > /tmp/fluidx3d_phase3.log 2>&1 &
```

Erwartete `print_info` Outputs:
- Far: Layer1=~692k cells, Layer2=~700k, Layer3=~700k → total ~2 Mio (vs Phase 2 ~692k)
- Near: Layer1=~3 Mio, Layer2=~3 Mio, Layer3=~3 Mio → total ~9 Mio (vs Phase 2 ~3 Mio)
- Forces erwartet: deutlich stärkere Reduktion als Phase 2's ~5%

### 2. Bei Phase 3 success: Forces vs OpenFOAM-Reference vergleichen
- User-Quote: "vom optischen im Vergleich zu OpenFOAM" → OpenFOAM ist die Reference
- Wenn Phase 3 ~600-800N Drag Range erreicht → near Time-Attack target → Pi-Tensor (Path IV) nicht mehr nötig
- ParaView side-by-side: aktuelle Production vs OpenFOAM VTKs

### 3. Bei Phase 3 nicht genug: Phase 4 — Spalding statt Werner-Wengle
- WW PowerLaw approximates Spalding aber Spalding ist accurater im viscous-log Übergang
- ~2 Stunden code change (Newton-Raphson für u+ inversion)
- Could push reduction zu ~10-15% Bereich

## Mittelfristig

### 4. Bouzidi-BB Reaktivierung (parked path, kompatibel mit WALL_VISC_BOOST)
- Sparse-Infrastructure ist already drin (`bouzidi_active_cells`)
- Just need to implement correct EP-pull-aware kernel
- Cherry-pick from `phase0-ahmed-validation` branch (sphere validation untested!)
- Adressiert Voxelisierungs-Staircase (Underbody Totwasser) — orthogonal zu WALL_VISC_BOOST

### 5. PERF-F V5 Multi-Stream PCIe (geplant, nicht implementiert)
- Phase A schon drin: 2. cl_transfer_queue in opencl.hpp
- Phase B/C: hide PCIe behind compute → ~2-3% gain
- Aufwand: ~1 Tag

### 6. Higher Resolution Far (User-Idee von gestern)
- Y-Shrink Far auf 6m (-2.6 GB VRAM)
- Bei 3:1 ratio: 4.5mm Near + 13.5mm Far oder 4mm Near + 12mm Far
- VRAM budget allows kleinen resolution increase

## Falls Wall-Function-Pfad nicht ausreicht

### 7. Path IV Pi-Tensor (Han 2021 / Malaspinas-Sagaut 2014)
- Already designed: `findings/PATH_IV_PI_TENSOR_DESIGN_2026-05-15.md`
- Mathematisch sauberer wall function: f = f_eq(u_wall) + f_neq(τ_w)
- 1-2 Wochen Implementation
- Known NaN risks (OpenLB forum)

## Bug-Fixes / Cleanup

### 8. Watcher + Manual VTK-Fix Konflikt (HEUTE ERLEBT)
- Watcher auto-applied fix_vtk_mesh AND manual application = double-shift
- Lösung: Marker-File neben mesh-vtk OR checksum/idempotency check in fix_vtk_mesh.py
- Quick fix: write a `.fixed` sidecar file when fix_vtk_mesh succeeds, skip if exists

### 9. ParaView Wayland Crash (HEUTE 2× erlebt)
- Workaround: `QT_QPA_PLATFORM=xcb setsid /home/heiko/opt/paraview-gpu/bin/paraview ...`
- Bereits in Memory festgehalten
- Permanent fix: wrapper-script `/home/heiko/opt/paraview-gpu/bin/paraview-xcb.sh`

## Current Code State

- Branch: `plan-refresh-multires` (gepusht zu remote)
- Latest commit: `4cccdaf` (Vehicle+Floor populate fix + production results)
- Pending uncommitted: Phase 3 multi-cell extension (lbm.cpp populate_wall_adj_flag + kernel.cpp Layer-scaled nu_t)
- Build verifiziert: kompilliert ohne Fehler

## See Also

- `findings/SESSION_2026-05-15_SUMMARY.md` — heute's complete summary
- `findings/NEXT_STEPS_PLAN_2026-05-15.md` — master plan
- `findings/WALL_TREATMENT_SUMMARY_2026-05-15.md` — 4-approach failure analysis
- `findings/WALL_MODEL_RESEARCH.md` — älteres research-doc
